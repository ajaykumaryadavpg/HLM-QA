import { test, expect } from '/Users/ajaykumar.yadav/tools/playwright-setup/node_modules/@playwright/test';

test.describe('Test group', () => {
  test('seed', async ({ page }) => {
    // Seed test - just setup the browser
  });
});