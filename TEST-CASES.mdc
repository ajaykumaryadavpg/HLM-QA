# HLM-QA Test Case Catalogue

**Project:** HLM Inventory Management — QA Suite
**Generated:** 2026-04-02
**Total Tests:** 246

---

## Table of Contents

1. [UI Tests — Login](#1-ui-tests--login)
2. [UI Tests — Dashboard KPI Cards](#2-ui-tests--dashboard-kpi-cards)
3. [UI Tests — Dashboard API Data Layer](#3-ui-tests--dashboard-api-data-layer)
4. [UI Tests — Geo Location Map](#4-ui-tests--geo-location-map)
5. [API Tests — Devices](#5-api-tests--devices)
6. [API Tests — Service Orders](#6-api-tests--service-orders)
7. [API Tests — Firmware](#7-api-tests--firmware)
8. [API Tests — Compliance](#8-api-tests--compliance)
9. [API Tests — Audit Logs](#9-api-tests--audit-logs)
10. [API Tests — Users & Customers](#10-api-tests--users--customers)
11. [API Tests — Mutations](#11-api-tests--mutations)
12. [UI Tests — Dashboard Recent Alerts](#12-ui-tests--dashboard-recent-alerts)
13. [UI Tests — Dashboard System Status](#13-ui-tests--dashboard-system-status)
14. [UI Tests — Dashboard Alerts & System Status Integration](#14-ui-tests--dashboard-alerts--system-status-integration)
15. [UI Tests — Dashboard Quick Actions with Live Counts](#15-ui-tests--dashboard-quick-actions-with-live-counts)
16. [UI Tests — Dashboard E2E Integration (PS-7)](#16-ui-tests--dashboard-e2e-integration-ps-7)

---

## 1. UI Tests — Login

**Suite File:** `inventory-login-suite.xml`
**Test Class:** `InventoryLoginTests`
**Groups:** `SMOKE_TESTS`, `INVENTORY_LOGIN`, `NEGATIVE`

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| UI-LOGIN-001 | Successful login with valid credentials | 1. Clear browser cookies and session/local storage<br>2. Launch the HLM Platform login page<br>3. Verify login page heading 'Sign in to your account' is visible<br>4. Enter valid admin email and password, click Sign In<br>5. Verify Dashboard h1 heading is visible after redirect<br>6. Verify 'Inventory & Assets' nav link is present in sidebar<br>7. Verify welcome message contains 'Welcome back' | User is redirected to the Dashboard; h1 header, nav link, and welcome message are all visible | SMOKE | Active |
| UI-LOGIN-002 | Login fails with invalid credentials | 1. Clear session state and launch login page<br>2. Enter invalid email and invalid password<br>3. Click Sign In<br>4. Verify red error banner (p.text-red-400) is visible<br>5. Verify Sign In button is still present (no redirect occurred) | Red error banner is displayed; user remains on the login page | NEGATIVE | Active |
| UI-LOGIN-003 | Login with empty email stays on login page | 1. Clear session state and launch login page<br>2. Leave email field empty<br>3. Enter valid password<br>4. Click Sign In<br>5. Verify Sign In button still visible (HTML5 blocked empty email)<br>6. Verify login page heading is still present | HTML5 validation blocks form submission; user remains on login page | NEGATIVE | Active |
| UI-LOGIN-004 | Login with empty password stays on login page | 1. Clear session state and launch login page<br>2. Enter valid email address<br>3. Leave password field empty<br>4. Click Sign In<br>5. Verify Sign In button still visible (no redirect) | HTML5 validation blocks form submission; user remains on login page | NEGATIVE | Active |
| UI-LOGIN-005 | Login with both fields empty stays on login page | 1. Clear session state and launch login page<br>2. Click Sign In without filling any credentials<br>3. Verify Sign In button still visible<br>4. Verify login page heading is still present | HTML5 validation blocks empty form; user stays on login page | NEGATIVE | Active |

---

## 2. UI Tests — Dashboard KPI Cards

**Suite File:** `dashboard-kpi-suite.xml`
**Test Class:** `DashboardKpiTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_KPI`

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| UI-KPI-001 | Four KPI cards visible after login | 1. Re-navigate to Dashboard for a clean state<br>2. Verify 'Welcome back' h2 heading is visible<br>3. Verify Total Devices KPI card value element is visible<br>4. Verify Active Deployments KPI card value element is visible<br>5. Verify Pending Approvals KPI card value element is visible<br>6. Verify Health Score KPI card value element is visible<br>7. Verify no red error banner is present<br>8. Verify no '—' loading placeholder remains | All four KPI cards are visible, populated, and error-free | SMOKE | Active |
| UI-KPI-002 | Total Devices card has blue icon and numeric count | 1. Verify 'Total Devices' card label (div.text-sm) is visible<br>2. Verify Total Devices numeric value (div.text-3xl) is visible<br>3. Verify blue SVG Package icon (text-blue-* class) is present<br>4. Verify no '—' loading placeholder remains | Total Devices card shows label, blue icon, and a resolved numeric count | SMOKE | Active |
| UI-KPI-003 | Active Deployments card has green icon and in-progress count | 1. Verify 'Active Deployments' card label is visible<br>2. Verify Active Deployments numeric value is visible<br>3. Verify green SVG Download icon (text-green-* class) is present<br>4. Verify no loading placeholder remains | Active Deployments card shows label, green icon, and resolved count | SMOKE | Active |
| UI-KPI-004 | Pending Approvals card has orange icon and combined count | 1. Verify 'Pending Approvals' card label is visible<br>2. Verify Pending Approvals numeric value (combined firmware + compliance) is visible<br>3. Verify orange SVG Shield icon (text-orange-* class) is present<br>4. Verify no loading placeholder remains | Pending Approvals card shows label, orange icon, and aggregated count | SMOKE | Active |
| UI-KPI-005 | Health Score card has green icon and percentage format | 1. Verify 'Health Score' card label is visible<br>2. Verify Health Score value contains '%' suffix (e.g., '77%')<br>3. Verify green SVG Check-circle icon (text-green-* class) is present<br>4. Verify no loading placeholder remains | Health Score card shows label, green icon, and a value in N% format | SMOKE | Active |
| UI-KPI-006 | KPI cards show em-dash loading placeholder on page load | 1. Register route handler to delay all AppSync GraphQL responses by 2 s<br>2. Re-navigate to Dashboard URL to trigger fresh page load<br>3. Verify em-dash '—' (U+2014) is visible in at least one KPI card<br>4. Unregister route handler (cleanup via finally block) | At least one KPI card shows '—' placeholder while APIs are in-flight | REGRESSION | Active |
| UI-KPI-007 | KPI cards show zero fallback when no data exists | 1. Navigate to Dashboard, wait for live data to confirm loaded state<br>2. Register route to return empty arrays for all fetch/xhr requests<br>3. Click Refresh Dashboard to re-trigger all KPI APIs<br>4. Verify no error banner is shown<br>5. Verify Total Devices card shows '0'<br>6. Verify Active Deployments card shows '0'<br>7. Verify Pending Approvals card shows '0'<br>8. Verify Health Score card shows '0%'<br>9. Unregister route handler (cleanup) | All KPI cards display '0' or '0%' with no error banner when APIs return empty data | REGRESSION | Active |
| UI-KPI-008 | KPI cards render in 4-column layout at desktop viewport | 1. Verify KPI grid container (div.grid) is visible at current desktop viewport<br>2. Verify all four KPI card values are visible without horizontal scrolling<br>3. Evaluate via JS whether grid-cols-4 Tailwind class is active on grid container<br>4. Assert KPI grid container is present confirming 4-column layout | All four cards visible in a single 4-column row at 1280px+ width | REGRESSION | Active |
| UI-KPI-009 | KPI cards render in 2×2 grid at tablet viewport | 1. Resize browser viewport to 768×1024 (tablet)<br>2. Re-navigate to Dashboard to trigger reflow<br>3. Verify Total Devices card is visible at tablet width<br>4. Verify Active Deployments, Pending Approvals, Health Score cards are visible<br>5. Restore viewport to 1620×1080 (desktop) | Cards reflow into a 2×2 grid; all four remain visible at tablet width | REGRESSION | Active |
| UI-KPI-010 | KPI cards stack in single column at mobile viewport | 1. Resize browser viewport to 375×667 (mobile)<br>2. Re-navigate to Dashboard to trigger reflow<br>3. Verify Total Devices card is visible at mobile width<br>4. Verify all four KPI cards are accessible without horizontal scrolling<br>5. Restore viewport to 1620×1080 (desktop) | All four cards stack vertically in a single column at 375px width | REGRESSION | Active |
| UI-KPI-011 | Refresh Dashboard button re-fetches all KPI data | 1. Verify Dashboard is loaded with live data (Total Devices card visible)<br>2. Click the Refresh Dashboard button<br>3. Verify all four KPI cards are populated after refresh<br>4. Verify no error banner is present post-refresh | All KPI cards update with fresh data after clicking Refresh Dashboard | SMOKE | Active |
| UI-KPI-012 | API failure shows red error banner and KPI fallback values | 1. Register route to return HTTP 500 from GraphQL endpoint<br>2. Navigate to Dashboard with API failing<br>3. Verify red error banner container (div.bg-red-50) is visible<br>4. Verify error banner message is visible<br>5. Unregister route handler (cleanup) | Red error banner appears with message; KPI cards show fallback values | REGRESSION | Active |
| UI-KPI-013 | KPI cards render correctly in dark mode | 1. Activate dark mode on the Dashboard<br>2. Verify all four KPI card value elements are visible<br>3. Verify all four KPI card label elements are visible<br>4. Verify all four KPI card icon elements are visible<br>5. Verify no error banner is present in dark mode | All KPI card icons, labels, and values render correctly with dark mode active | REGRESSION | Active |
| UI-KPI-014 | Health Score value conforms to rounded percentage format | 1. Verify Health Score KPI card value element is visible<br>2. Assert value text matches pattern `^\d{1,3}%$` (e.g., '0%', '77%', '100%')<br>3. Verify no loading placeholder '—' remains | Health Score displays as a rounded integer percentage with no decimals | REGRESSION | Active |
| UI-KPI-015 | KPI card icon colors match specification | 1. Verify Total Devices icon has a blue Tailwind class (text-blue-*)<br>2. Verify Active Deployments icon has a green Tailwind class (text-green-*)<br>3. Verify Pending Approvals icon has an orange Tailwind class (text-orange-*)<br>4. Verify Health Score icon has a green Tailwind class (text-green-*) | Each KPI card icon renders with the correct specification colour class | REGRESSION | Active |
| UI-KPI-016 | KPI values are never negative or invalid artifacts | 1. Read text content of Total Devices value element<br>2. Assert value is a non-negative integer string<br>3. Read Active Deployments, Pending Approvals values and assert ≥ 0<br>4. Read Health Score value and assert it matches N% with 0 ≤ N ≤ 100<br>5. Verify no value is 'NaN', 'undefined', 'null', or empty | All KPI values are valid non-negative numbers; no JavaScript artifacts present | REGRESSION | Active |
| UI-KPI-017 | Unauthenticated access to dashboard redirects to login | 1. Clear all cookies and browser storage to remove auth session<br>2. Navigate directly to the Dashboard URL<br>3. Verify Sign In button is visible (redirected to login page)<br>4. Verify login page heading is visible<br>5. Verify Dashboard header is NOT present | Unauthenticated navigation to dashboard redirects to login page | SMOKE | Active |
| UI-KPI-018 | Pending Approvals displays sum of firmware and compliance counts | 1. Intercept listFirmware and listCompliance GraphQL requests and capture responses<br>2. Count records with status='Pending' from firmware API response<br>3. Count records with status='Pending' from compliance API response<br>4. Read Pending Approvals KPI card displayed value<br>5. Assert displayed value = pending firmware count + pending compliance count | Pending Approvals KPI equals the arithmetic sum of pending firmware + compliance records | REGRESSION | Active |

---

## 3. UI Tests — Dashboard API Data Layer

**Suite File:** `dashboard-api-suite.xml`
**Test Class:** `DashboardApiTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_API`

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| UI-DAPI-001 | Parallel API calls on dashboard load | 1. Navigate to Dashboard and trigger fresh page load<br>2. Verify Total Devices KPI value is visible (devices API responded)<br>3. Verify Active Deployments KPI value is visible (service orders API responded)<br>4. Verify Pending Approvals KPI value is visible (firmware + compliance APIs responded)<br>5. Verify no loading placeholder '—' remains | All six data-domain APIs respond and their values are reflected across KPI cards | REGRESSION | Active |
| UI-DAPI-002 | All device records are fetched | 1. Verify Total Devices KPI card value element is present in the DOM<br>2. Verify no loading placeholder '—' remains — data has fully loaded | Total Devices KPI card displays a resolved numeric count | SMOKE | Active |
| UI-DAPI-003 | Offline devices are fetched separately | 1. Verify 'View Inventory' card is present in the Quick Actions section<br>2. Verify orange offline-devices badge (span.bg-orange-500) is visible inside the 'View Inventory' card | Offline device count badge is visible on the Quick Actions Inventory card | REGRESSION | Active |
| UI-DAPI-004 | In-progress service orders are fetched | 1. Verify Active Deployments KPI card value element is present<br>2. Verify no loading placeholder '—' remains across all KPI cards | Active Deployments KPI card shows a resolved count after APIs respond | SMOKE | Active |
| UI-DAPI-005 | Scheduled service orders are fetched | 1. Verify 'Scheduled Service' card is present in Quick Actions section<br>2. Verify orange scheduled-orders badge (span.bg-orange-500) is visible inside the 'Scheduled Service' card | Scheduled orders count badge is visible on the Quick Actions Scheduled Service card | REGRESSION | Active |
| UI-DAPI-006 | Pending firmware records are fetched | 1. Verify Pending Approvals KPI card value element is visible<br>2. Verify 'Check Compliance' quick action link is visible | Pending Approvals KPI includes pending firmware count; compliance link is present | SMOKE | Active |
| UI-DAPI-007 | Pending compliance records are fetched | 1. Verify Pending Approvals KPI card value element is visible (combined firmware + compliance)<br>2. Verify no loading placeholder '—' remains in Pending Approvals card | Pending Approvals KPI reflects both pending firmware and pending compliance counts | REGRESSION | Active |
| UI-DAPI-008 | KPI cards show loading placeholder on page load | 1. Register route to delay all GraphQL responses by 2 s<br>2. Navigate directly to the Dashboard URL<br>3. Verify '—' (em-dash, U+2014) is visible in at least one KPI card during fetch<br>4. Unregister route handler (cleanup via finally block) | Em-dash placeholder is visible in KPI cards while API responses are pending | REGRESSION | Active |
| UI-DAPI-009 | KPI cards transition from placeholder to live values | 1. Verify Total Devices KPI value is present (not placeholder)<br>2. Verify Active Deployments KPI value is present<br>3. Verify Pending Approvals KPI value is present<br>4. Verify no loading placeholder '—' remains across any KPI card | All KPI cards display live data values; no placeholder remains after load | SMOKE | Active |
| UI-DAPI-010 | Error banner displayed on single API failure | 1. Register route to return HTTP 500 from GraphQL endpoint<br>2. Navigate to Dashboard with API returning 500<br>3. Verify red error banner container (div.bg-red-50) is visible<br>4. Verify error message text is visible within the banner<br>5. Unregister route handler (cleanup) | Red error banner with message appears when any API returns a server error | REGRESSION | Active |
| UI-DAPI-011 | Error banner and placeholders when all APIs fail | 1. Register route to abort all GraphQL requests (network-level failure)<br>2. Navigate to Dashboard with all APIs unavailable<br>3. Verify red error banner is visible<br>4. Verify KPI cards show '0' fallback (app resolved error state)<br>5. Verify Dashboard header is still rendered (no page crash)<br>6. Unregister route handler (cleanup) | Error banner shown; KPI cards show fallback '0'; dashboard does not crash | REGRESSION | Active |
| UI-DAPI-012 | Error banner displays meaningful message | 1. Register route to return HTTP 500 with specific error message payload<br>2. Navigate to Dashboard with error route active<br>3. Verify red error banner container is visible<br>4. Verify error banner message element is visible and non-empty<br>5. Unregister route handler (cleanup) | Error banner contains a visible, non-empty, API-specific error message | REGRESSION | Active |
| UI-DAPI-013 | Audit logs fetched for alerts panel | 1. Register route to intercept listAuditLogs GraphQL query and inject a mock audit log entry<br>2. Re-navigate to Dashboard so mock response is applied<br>3. Verify Recent Alerts panel container is visible<br>4. Verify at least one audit log list item is rendered in the panel<br>5. Unregister route handler (cleanup) | Recent Alerts panel is visible and shows at least one audit log entry | SMOKE | Active |
| UI-DAPI-014 | Audit logs 24-hour time window | 1. Register route to inject a mock audit log entry within last 24 hours<br>2. Re-navigate to Dashboard so mock response is applied<br>3. Verify Recent Alerts panel container is visible<br>4. Verify alert items from within 24-hour window are displayed<br>5. Verify 'No recent activity' message is NOT shown<br>6. Unregister route handler (cleanup) | Audit log entries from last 24 hours appear in the Alerts panel | REGRESSION | Active |
| UI-DAPI-015 | Alerts loading indicator displayed while fetching | 1. Register route to delay listAuditLogs GraphQL response by 2 s<br>2. Re-navigate to Dashboard<br>3. Verify loading indicator (spinner or skeleton) is visible in Alerts panel during fetch<br>4. Unregister route handler (cleanup) | A loading indicator is shown in the Alerts panel while audit log fetch is in-flight | REGRESSION | Active |
| UI-DAPI-016 | Alerts error message on API failure | 1. Register route to abort listAuditLogs GraphQL request<br>2. Navigate to Dashboard with alerts API failing<br>3. Verify Alerts panel container is visible<br>4. Verify error message is displayed inside the Alerts panel<br>5. Unregister route handler (cleanup) | Alerts panel shows a descriptive error message when its API call fails | REGRESSION | Active |
| UI-DAPI-017 | No dashboard auto-refresh or polling | 1. Navigate to Dashboard and wait for full load<br>2. Capture initial KPI values<br>3. Wait 35 seconds without interaction<br>4. Verify no additional API calls have been triggered<br>5. Verify KPI values remain unchanged (no auto-refresh occurred) | Dashboard data does not refresh automatically; values remain static until user action | REGRESSION | Active |
| UI-DAPI-018 | Device list is capped at 100 records | 1. Navigate to Dashboard and verify Total Devices KPI is visible<br>2. Intercept the listDevices GraphQL request and inspect the limit variable<br>3. Assert that limit ≤ 100 in the API request<br>4. Verify Total Devices KPI value is ≤ 100 | The listDevices API call is sent with a limit of 100 or fewer records | REGRESSION | Active |
| UI-DAPI-019 | Dashboard loads with zero data | 1. Register route to return empty items arrays for all dashboard APIs<br>2. Navigate to Dashboard<br>3. Verify no red error banner appears<br>4. Verify all four KPI cards show '0' or '0%'<br>5. Verify dashboard header is still rendered<br>6. Unregister route handler (cleanup) | Dashboard renders cleanly with all-zero KPI values when APIs return empty datasets | REGRESSION | Active |
| UI-DAPI-020 | KPI and alerts fetch independently | 1. Register route to abort ONLY the listAuditLogs GraphQL request<br>2. Navigate to Dashboard with alerts API failing<br>3. Verify KPI cards are populated (Total Devices, Active Deployments, Pending Approvals, Health Score)<br>4. Verify error message appears in Alerts panel only<br>5. Unregister route handler (cleanup) | KPI cards load successfully even when the Alerts panel API fails | REGRESSION | Active |
| UI-DAPI-021 | Refresh Dashboard button triggers complete data refetch | 1. Verify Dashboard is loaded with initial data<br>2. Click the Refresh Dashboard button (circular arrow icon)<br>3. Verify loading placeholders briefly appear in KPI cards<br>4. Verify all KPI cards repopulate with fresh data<br>5. Verify Alerts panel refreshes<br>6. Verify no error banner is present post-refresh | Clicking Refresh triggers re-fetch of all APIs; all panels update | SMOKE | Active |
| UI-DAPI-022 | Refresh button visible with circular arrow icon | 1. Verify Dashboard is loaded<br>2. Verify the Refresh Dashboard button element is visible on the page<br>3. Verify the button contains a circular arrow SVG icon | Refresh Dashboard button is visible with a circular arrow icon | REGRESSION | Active |
| UI-DAPI-023 | KPI cards show placeholders during refresh | 1. Navigate to Dashboard and wait for full load<br>2. Register route to delay GraphQL responses by 2 s<br>3. Click the Refresh Dashboard button<br>4. Immediately verify KPI cards show '—' loading placeholders<br>5. Unregister route handler (cleanup) | KPI cards revert to '—' loading state while a manual refresh is in progress | REGRESSION | Active |
| UI-DAPI-024 | Alerts panel shows loading state during refresh | 1. Navigate to Dashboard and wait for full load<br>2. Register route to delay listAuditLogs response by 2 s<br>3. Click the Refresh Dashboard button<br>4. Verify loading indicator appears in Alerts panel during fetch<br>5. Unregister route handler (cleanup) | Alerts panel shows a loading indicator while a manual refresh is fetching audit logs | REGRESSION | Active |
| UI-DAPI-025 | Error banners cleared when refresh starts | 1. Register route to return HTTP 500 from GraphQL endpoint<br>2. Navigate to Dashboard so error banner appears<br>3. Unregister error route and register success route<br>4. Click the Refresh Dashboard button<br>5. Verify the error banner is cleared immediately when refresh starts<br>6. Verify KPI cards repopulate with data after successful refresh | Any existing error banners are dismissed when Refresh Dashboard is clicked | REGRESSION | Active |
| UI-DAPI-026 | System Status panel displays four services | 1. Verify Dashboard is loaded<br>2. Verify System Status panel container is visible<br>3. Verify exactly four service rows are rendered inside the panel<br>4. Verify each row has a service name label and a status indicator | System Status panel renders with exactly four service entries | SMOKE | Active |
| UI-DAPI-027 | System Status health percentages update from live KPI data | 1. Wait for Dashboard to fully load<br>2. Read Total Devices and Health Score KPI values<br>3. Read health percentage values displayed in the System Status panel<br>4. Assert System Status health percentages correlate with live KPI data | System Status health percentages are derived from and aligned with live KPI values | REGRESSION | Active |
| UI-DAPI-028 | System Status operational and degraded labels | 1. Wait for Dashboard to fully load<br>2. Verify at least one service row with 100% health shows 'Operational' label<br>3. Verify any service row below health threshold shows 'Degraded' label | Service rows display 'Operational' or 'Degraded' labels based on health threshold | REGRESSION | Active |
| UI-DAPI-029 | Quick action badges hidden when count is zero | 1. Register route to return empty arrays for firmware and compliance APIs<br>2. Click Refresh Dashboard<br>3. Verify Quick Action Firmware badge is NOT visible<br>4. Verify Quick Action Compliance badge is NOT visible<br>5. Unregister route handler (cleanup) | Quick Action count badges are hidden when the corresponding pending count is zero | REGRESSION | Active |
| UI-DAPI-030 | Quick action badges visible when count is greater than zero | 1. Verify Dashboard is loaded with live data<br>2. Verify Pending Approvals KPI value is greater than zero<br>3. Verify Quick Action badge for Firmware or Compliance is visible with a count | Quick Action badges display numeric counts when pending items exist | SMOKE | Active |
| UI-DAPI-031 | No recent activity message when no audit logs exist | 1. Register route to return empty items for listAuditLogs query<br>2. Re-navigate to Dashboard so empty audit logs response is applied<br>3. Verify Alerts panel container is visible<br>4. Verify 'No recent activity' empty-state message is displayed<br>5. Verify no audit log list items are rendered<br>6. Unregister route handler (cleanup) | Alerts panel shows 'No recent activity' empty-state when audit log API returns no items | REGRESSION | Active |

---

## 4. UI Tests — Geo Location Map

**Suite File:** `geo-location-suite.xml`
**Test Class:** `GeoLocationTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `GEO_LOCATION`

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| UI-GEO-001 | Geo Location tab renders interactive MapLibre map | 1. Navigate to Dashboard → Inventory module via sidebar<br>2. Wait for Inventory page heading to appear<br>3. Click the Geo Location tab<br>4. Wait for MapLibre map canvas and device pins to render<br>5. Verify `div.maplibregl-map` container is present<br>6. Verify HTML5 canvas element is rendered inside map container<br>7. Verify `[aria-label='Map']` region is present<br>8. Verify legacy static SVG map element is NOT present<br>9. Verify at least one `.maplibregl-marker` pin is visible<br>10. Verify MapLibre attribution link is visible | Interactive MapLibre map renders; canvas and markers present; no legacy SVG map | SMOKE | Active |
| UI-GEO-002 | Map legend displays correct status entries | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Verify 'Online' text entry is visible in the legend panel<br>3. Verify 'Offline' text entry is visible in the legend panel<br>4. Verify 'Maintenance' text entry is visible in the legend panel | Map legend shows Online, Offline, and Maintenance entries with colour indicators | SMOKE | Active |
| UI-GEO-003 | All twelve pins visible with correct color distribution | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Verify 'All' filter button is active by default<br>3. Verify at least one map marker is visible<br>4. Evaluate via JS: count background-color of all pin dot divs<br>5. Assert total pin count = 12<br>6. Assert 6 pins rgb(34,197,94) green, 3 pins rgb(239,68,68) red, 3 pins rgb(249,115,22) orange | 12 pins rendered; JS color evaluation confirms 6 green, 3 red, 3 orange distribution | SMOKE | Active |
| UI-GEO-004 | Online filter shows six green pins | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Click 'Online(6)' filter button<br>3. Verify Online filter button is active/highlighted<br>4. Wait for filtered pins to appear in DOM<br>5. Evaluate via JS: count all map marker elements → assert = 6<br>6. Evaluate via JS: count green pins rgb(34,197,94) → assert = 6 | Exactly 6 green pins visible after Online filter; all have correct green colour | REGRESSION | Active |
| UI-GEO-005 | Offline filter shows three red pins | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Click 'Offline(3)' filter button<br>3. Wait for filtered pins to appear in DOM<br>4. Evaluate via JS: count all map marker elements → assert = 3<br>5. Evaluate via JS: count red pins rgb(239,68,68) → assert = 3 | Exactly 3 red pins visible after Offline filter; all have correct red colour | REGRESSION | Active |
| UI-GEO-006 | Maintenance filter shows three orange pins | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Click 'Maintenance(3)' filter button<br>3. Wait for filtered pins to appear in DOM<br>4. Evaluate via JS: count all map marker elements → assert = 3<br>5. Evaluate via JS: count orange pins rgb(249,115,22) → assert = 3 | Exactly 3 orange pins visible after Maintenance filter; all have correct orange colour | REGRESSION | Active |
| UI-GEO-007 | Stat pills display correct device counts | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Verify Total, Online, Offline, Maintenance stat pills are all visible<br>3. Verify Online filter button label contains '6'<br>4. Verify Offline filter button label contains '3'<br>5. Verify Maintenance filter button label contains '3' | All four stat pills visible; counts match Online(6), Offline(3), Maintenance(3) | SMOKE | Active |
| UI-GEO-008 | All four filter buttons are present | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Verify 'All' filter button is visible and active by default<br>3. Verify 'Online(N)' filter button is visible with count suffix<br>4. Verify 'Offline(N)' filter button is visible with count suffix<br>5. Verify 'Maintenance(N)' filter button is visible with count suffix | All four filter buttons (All, Online(6), Offline(3), Maintenance(3)) are present | SMOKE | Active |
| UI-GEO-009 | Filter sequence updates map markers correctly | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Assert initial pin count = 12 (All filter default)<br>3. Click Online filter → wait → assert pin count = 6<br>4. Click Offline filter → wait → assert pin count = 3<br>5. Click Maintenance filter → wait → assert pin count = 3<br>6. Click All filter → wait → assert pin count = 12 | Pin counts change correctly at each filter step: 12→6→3→3→12 | REGRESSION | Active |
| UI-GEO-010 | Maintenance pin opens detail card with correct fields | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Click Maintenance filter to show only orange pins<br>3. Click on the first Maintenance pin using `browser.evaluate()` (force click)<br>4. Wait for device detail card to appear<br>5. Verify detail card container is visible<br>6. Verify device name, status, address, coordinates, and last-seen fields are present | Clicking a Maintenance pin opens a detail card with all required device fields | REGRESSION | Active |
| UI-GEO-011 | Device detail card closes on X button click | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Click on any map marker to open the device detail card<br>3. Verify detail card is visible<br>4. Click the X (close) button on the detail card<br>5. Verify detail card container is no longer visible | Device detail card is dismissed when the X button is clicked | SMOKE | Active |
| UI-GEO-012 | Zoom in and zoom out controls are functional | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Evaluate via JS: get current map zoom level<br>3. Click the Zoom In (+) control button<br>4. Wait briefly for zoom animation<br>5. Evaluate via JS: assert new zoom level > initial zoom level<br>6. Click the Zoom Out (-) control button<br>7. Evaluate via JS: assert zoom level decreased | Zoom In increases map zoom level; Zoom Out decreases it | REGRESSION | Active |
| UI-GEO-013 | Compass button is present and interactive | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Verify compass/bearing reset button element is visible<br>3. Click the compass button<br>4. Verify no error is thrown and map remains interactive | Compass button is visible and responds to user clicks without error | REGRESSION | Active |
| UI-GEO-014 | Hardware Inventory tab unaffected by PS20 | 1. Navigate to Inventory module (via BeforeMethod)<br>2. Click the Hardware Inventory tab<br>3. Verify Hardware Inventory tab content is visible<br>4. Verify no MapLibre or Geo Location elements leak into Hardware Inventory view<br>5. Verify existing Hardware Inventory table/content renders correctly | Hardware Inventory tab content is unchanged and unaffected by PS20 changes | REGRESSION | Active |
| UI-GEO-015 | Firmware Status tab unaffected by PS20 | 1. Navigate to Inventory module (via BeforeMethod)<br>2. Click the Firmware Status tab<br>3. Verify Firmware Status tab content is visible<br>4. Verify no Geo Location UI elements are present in Firmware Status view<br>5. Verify existing Firmware Status content renders correctly | Firmware Status tab content is unchanged and unaffected by PS20 changes | REGRESSION | Active |
| UI-GEO-016 | Stat pill counts are static across filter changes | 1. Navigate to Geo Location tab (via BeforeMethod)<br>2. Read Online stat pill count (should be 6)<br>3. Click Online filter → assert Online stat pill still shows 6<br>4. Click Offline filter → assert Offline stat pill still shows 3<br>5. Click Maintenance filter → assert Maintenance stat pill still shows 3<br>6. Click All filter and verify all counts unchanged | Stat pill counts remain fixed at 6/3/3 regardless of which filter is active | REGRESSION | Active |

---

## 5. API Tests — Devices

**Spec File:** `specs/api/device.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-DEV-001 | listDevices returns paginated response with items | 1. Send authenticated GraphQL query: `listDevices` (no filter)<br>2. Assert HTTP 200 status code<br>3. Assert no errors in response<br>4. Assert listDevices result object is defined<br>5. Parse items array and assert length > 0 | Response is HTTP 200 with a populated items array | SMOKE | Active |
| API-DEV-002 | listDevices with limit returns at most N items | 1. Send authenticated GraphQL query: `listDevices(limit: 3)`<br>2. Assert HTTP 200 status code<br>3. Parse items array<br>4. Assert items.length ≤ 3 | Response contains at most 3 items when limit=3 is specified | REGRESSION | Active |
| API-DEV-003 | listDevices filters by status=Online | 1. Send authenticated GraphQL query: `listDevices(status: "Online")`<br>2. Assert HTTP 200 status code<br>3. Parse items array<br>4. Assert every item has `status === "Online"` | All returned items have status='Online' | REGRESSION | Active |
| API-DEV-004 | listDevices filters by status=Offline | 1. Send authenticated GraphQL query: `listDevices(status: "Offline")`<br>2. Assert HTTP 200<br>3. Parse items and assert every item has `status === "Offline"` | All returned items have status='Offline' | REGRESSION | Active |
| API-DEV-005 | listDevices filters by status=Maintenance | 1. Send authenticated GraphQL query: `listDevices(status: "Maintenance")`<br>2. Assert HTTP 200<br>3. Parse items and assert every item has `status === "Maintenance"` | All returned items have status='Maintenance' | REGRESSION | Active |
| API-DEV-006 | listDevices response items have required fields | 1. Send authenticated GraphQL query: `listDevices(limit: 1)`<br>2. Assert HTTP 200<br>3. Parse first item<br>4. Assert item.id, item.deviceName, item.serialNumber, item.model, item.status, item.location are all present | Each device item contains all required fields | SMOKE | Active |
| API-DEV-007 | listDevices totalCount is a non-negative integer | 1. Send authenticated GraphQL query: `listDevices`<br>2. Assert HTTP 200<br>3. Read `totalCount` from response<br>4. Assert `totalCount` is a number and totalCount ≥ 0 | totalCount field is present and is a non-negative integer | REGRESSION | Active |
| API-DEV-008 | getDevice returns device by valid ID | 1. Seed `firstDeviceId` in beforeAll by calling `listDevices`<br>2. Send authenticated GraphQL query: `getDevice(id: firstDeviceId)`<br>3. Assert HTTP 200<br>4. Assert no errors in response<br>5. Assert returned `device.id === firstDeviceId` | getDevice returns the complete device record for the queried ID | SMOKE | Active |
| API-DEV-009 | getDevicesByCustomer returns devices for a customer | 1. Seed `firstCustomerId` in beforeAll<br>2. Send authenticated GraphQL query: `getDevicesByCustomer(customerId: firstCustomerId)`<br>3. Assert HTTP 200<br>4. Parse items array and assert length > 0 | All devices belonging to the specified customer are returned | REGRESSION | Active |
| API-DEV-010 | getDevicesByLocation returns devices for a location | 1. Seed `firstLocation` in beforeAll<br>2. Send authenticated GraphQL query: `getDevicesByLocation(location: firstLocation)`<br>3. Assert HTTP 200<br>4. Parse items array and assert length > 0 | All devices at the specified location are returned | REGRESSION | Active |
| API-DEV-011 | getDevice with non-existent ID returns null or error | 1. Send authenticated GraphQL query: `getDevice(id: "non-existent-id-999")`<br>2. Assert HTTP 200<br>3. Assert `response.data.getDevice` is null OR `response.errors` is defined | API returns null data or a descriptive error for an unknown device ID | NEGATIVE | Active |
| API-DEV-012 | listDevices with invalid status returns empty items or error | 1. Send authenticated GraphQL query: `listDevices(status: "INVALID_STATUS")`<br>2. Assert HTTP 200<br>3. Assert items array is empty OR response contains a validation error | Invalid status filter returns empty items or a GraphQL validation error | NEGATIVE | Active |
| API-DEV-013 | getDevicesByCustomer with non-existent customer returns empty items | 1. Send authenticated GraphQL query: `getDevicesByCustomer(customerId: "non-existent-cust-999")`<br>2. Assert HTTP 200<br>3. Parse items array and assert length === 0 | Empty items array returned for an unknown customer ID | NEGATIVE | Active |
| API-DEV-014 | getDevicesByLocation with unknown location returns empty items | 1. Send authenticated GraphQL query: `getDevicesByLocation(location: "UNKNOWN_LOCATION_XYZ")`<br>2. Assert HTTP 200<br>3. Parse items array and assert length === 0 | Empty items array returned for an unknown location | NEGATIVE | Active |
| API-DEV-015 | Unauthenticated listDevices request returns 401 or auth error | 1. Build GraphQL request for `listDevices` WITHOUT authentication token<br>2. Send unauthenticated request to the GraphQL endpoint<br>3. Assert HTTP 401 OR response contains an auth error | Unauthenticated request is rejected with 401 or an auth error | NEGATIVE | Active |

---

## 6. API Tests — Service Orders

**Spec File:** `specs/api/service-orders.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-SO-001 | listServiceOrdersByStatus returns Pending orders | 1. Send authenticated GraphQL query: `listServiceOrdersByStatus(status: "Pending")`<br>2. Assert HTTP 200 and no errors<br>3. Parse items array and assert length > 0<br>4. Assert all items have `status === "Pending"` | Only Pending service orders are returned | SMOKE | Active |
| API-SO-002 | listServiceOrdersByStatus returns InProgress orders | 1. Send authenticated GraphQL query: `listServiceOrdersByStatus(status: "In Progress")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "In Progress"` | Only In Progress service orders are returned | REGRESSION | Active |
| API-SO-003 | listServiceOrdersByStatus returns Completed orders | 1. Send authenticated GraphQL query: `listServiceOrdersByStatus(status: "Completed")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Completed"` | Only Completed service orders are returned | REGRESSION | Active |
| API-SO-004 | listServiceOrdersByStatus returns Scheduled orders | 1. Send authenticated GraphQL query: `listServiceOrdersByStatus(status: "Scheduled")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Scheduled"` | Only Scheduled service orders are returned | REGRESSION | Active |
| API-SO-005 | listServiceOrdersByStatus response items have required fields | 1. Send authenticated GraphQL query: `listServiceOrdersByStatus` with any valid status<br>2. Assert HTTP 200<br>3. Parse first item<br>4. Assert item.id, item.title, item.status, item.technicianId, item.scheduledDate are all present | Each service order item contains all required fields | SMOKE | Active |
| API-SO-006 | listServiceOrdersByDate returns results within last 30 days | 1. Compute `startDate = now - 30 days`, `endDate = now` (ISO format)<br>2. Send authenticated GraphQL query: `listServiceOrdersByDate(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Parse items and assert all scheduledDate values fall within the 30-day range | All returned orders have scheduledDate within the last 30 days | REGRESSION | Active |
| API-SO-007 | listServiceOrdersByDate with future range returns empty items | 1. Compute `startDate = now + 1 year`, `endDate = now + 2 years`<br>2. Send authenticated GraphQL query: `listServiceOrdersByDate(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Parse items and assert length === 0 | Empty items array returned for a fully future date range | NEGATIVE | Active |
| API-SO-008 | getServiceOrder returns order by valid ID | 1. Seed a valid service order ID from `listServiceOrdersByStatus`<br>2. Send authenticated GraphQL query: `getServiceOrder(id: seededId)`<br>3. Assert HTTP 200<br>4. Assert returned `order.id === seededId` | Complete service order record returned for the queried ID | SMOKE | Active |
| API-SO-009 | getServiceOrdersByTechnician returns orders for a technician | 1. Seed a valid `technicianId` from `listServiceOrdersByStatus`<br>2. Send GraphQL query: `getServiceOrdersByTechnician(technicianId: seededId)`<br>3. Assert HTTP 200<br>4. Parse items and assert length > 0 | All orders for the specified technician are returned | REGRESSION | Active |
| API-SO-010 | getServiceOrder with non-existent ID returns null or error | 1. Send authenticated GraphQL query: `getServiceOrder(id: "non-existent-so-999")`<br>2. Assert HTTP 200<br>3. Assert `response.data.getServiceOrder` is null OR `response.errors` is defined | null data or descriptive error returned for unknown service order ID | NEGATIVE | Active |
| API-SO-011 | listServiceOrdersByStatus with invalid status returns empty or error | 1. Send authenticated GraphQL query: `listServiceOrdersByStatus(status: "FAKE_STATUS")`<br>2. Assert HTTP 200<br>3. Assert items array is empty OR `response.errors` is defined | Invalid status returns empty items or a validation error | NEGATIVE | Active |
| API-SO-012 | getServiceOrdersByTechnician with unknown ID returns empty items | 1. Send GraphQL query: `getServiceOrdersByTechnician(technicianId: "unknown-tech-999")`<br>2. Assert HTTP 200<br>3. Parse items and assert length === 0 | Empty items array returned for unknown technician ID | NEGATIVE | Active |
| API-SO-013 | listServiceOrdersByDate with endDate before startDate returns empty or error | 1. Set `startDate = now`, `endDate = now - 1 day`<br>2. Send authenticated GraphQL query: `listServiceOrdersByDate(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Assert items is empty OR `response.errors` is defined | Reversed date range returns empty results or a validation error | NEGATIVE | Active |

---

## 7. API Tests — Firmware

**Spec File:** `specs/api/firmware.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-FW-001 | listFirmware returns all firmware with expected fields | 1. Send authenticated GraphQL query: `listFirmware` (no filter)<br>2. Assert HTTP 200<br>3. Assert no errors in response<br>4. Parse items and assert length > 0 | Response is HTTP 200 with a populated firmware items array | SMOKE | Active |
| API-FW-002 | listFirmware response items have required fields | 1. Send authenticated GraphQL query: `listFirmware(limit: 1)`<br>2. Assert HTTP 200<br>3. Parse first item<br>4. Assert item.id, item.name, item.version, item.status, item.deviceModel are present and non-null | Each firmware item contains all required fields | SMOKE | Active |
| API-FW-003 | listFirmware filters by status=Pending | 1. Send authenticated GraphQL query: `listFirmware(status: "Pending")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Pending"` | Only Pending firmware records are returned | REGRESSION | Active |
| API-FW-004 | listFirmware filters by status=Approved | 1. Send authenticated GraphQL query: `listFirmware(status: "Approved")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Approved"` | Only Approved firmware records are returned | REGRESSION | Active |
| API-FW-005 | listFirmware filters by status=Deprecated | 1. Send authenticated GraphQL query: `listFirmware(status: "Deprecated")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Deprecated"` | Only Deprecated firmware records are returned | REGRESSION | Active |
| API-FW-006 | listFirmware with limit=2 returns at most 2 items | 1. Send authenticated GraphQL query: `listFirmware(limit: 2)`<br>2. Assert HTTP 200<br>3. Parse items array<br>4. Assert items.length ≤ 2 | Response contains at most 2 items when limit=2 is specified | REGRESSION | Active |
| API-FW-007 | listFirmware pagination: nextToken advances the cursor | 1. Send `listFirmware(limit: 2)` and capture `nextToken` from response<br>2. Assert `nextToken` is defined and non-null<br>3. Send `listFirmware(limit: 2, nextToken: capturedToken)`<br>4. Assert HTTP 200 and response contains the next page of items | nextToken from page 1 correctly advances to page 2 with different items | REGRESSION | Active |
| API-FW-008 | getFirmware returns firmware by valid ID | 1. Seed a valid firmware ID from `listFirmware` in beforeAll<br>2. Send authenticated GraphQL query: `getFirmware(id: seededId)`<br>3. Assert HTTP 200<br>4. Assert returned `firmware.id === seededId` | Complete firmware record returned for the queried ID | SMOKE | Active |
| API-FW-009 | getFirmwareByModel returns firmware for a known model | 1. Seed a valid `deviceModel` from `listFirmware` in beforeAll<br>2. Send authenticated GraphQL query: `getFirmwareByModel(deviceModel: seededModel)`<br>3. Assert HTTP 200<br>4. Parse items and assert length > 0 | Firmware records for the specified model are returned | REGRESSION | Active |
| API-FW-010 | getFirmwareWithRelations returns data for valid ID | 1. Seed a valid firmware ID from beforeAll<br>2. Send authenticated GraphQL query: `getFirmwareWithRelations(id: seededId)`<br>3. Assert HTTP 200<br>4. Assert returned data is non-null and includes related entity fields | Firmware record with related entities is returned for the queried ID | REGRESSION | Active |
| API-FW-011 | getFirmware with non-existent ID returns null or error | 1. Send authenticated GraphQL query: `getFirmware(id: "non-existent-fw-999")`<br>2. Assert HTTP 200<br>3. Assert `response.data.getFirmware` is null OR `response.errors` is defined | null data or descriptive error returned for unknown firmware ID | NEGATIVE | Active |
| API-FW-012 | getFirmwareByModel with unknown model returns empty items | 1. Send authenticated GraphQL query: `getFirmwareByModel(deviceModel: "UNKNOWN_MODEL_XYZ")`<br>2. Assert HTTP 200<br>3. Parse items and assert length === 0 | Empty items array returned for an unrecognised device model | NEGATIVE | Active |
| API-FW-013 | listFirmware with invalid status returns empty or error | 1. Send authenticated GraphQL query: `listFirmware(status: "INVALID_STATUS")`<br>2. Assert HTTP 200<br>3. Assert items is empty OR `response.errors` is defined | Invalid status returns empty items or a validation error | NEGATIVE | Active |
| API-FW-014 | getFirmwareWithRelations with non-existent ID returns null or error | 1. Send authenticated GraphQL query: `getFirmwareWithRelations(id: "non-existent-fw-999")`<br>2. Assert HTTP 200<br>3. Assert data is null OR `response.errors` is defined | null or error returned for unknown firmware ID with relations query | NEGATIVE | Active |
| API-FW-015 | Unauthenticated listFirmware request is rejected | 1. Build GraphQL request for `listFirmware` WITHOUT authentication token<br>2. Send unauthenticated request to the GraphQL endpoint<br>3. Assert HTTP 401 OR response contains an authentication error | Unauthenticated request is rejected with 401 or an auth error | NEGATIVE | Active |

---

## 8. API Tests — Compliance

**Spec File:** `specs/api/compliance.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-COMP-001 | listComplianceByStatus Approved returns only approved records | 1. Send authenticated GraphQL query: `listComplianceByStatus(status: "Approved")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Approved"` | Only Approved compliance records are returned | SMOKE | Active |
| API-COMP-002 | listComplianceByStatus Pending returns only pending records | 1. Send authenticated GraphQL query: `listComplianceByStatus(status: "Pending")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Pending"` | Only Pending compliance records are returned | SMOKE | Active |
| API-COMP-003 | listComplianceByStatus Deprecated returns only deprecated records | 1. Send authenticated GraphQL query: `listComplianceByStatus(status: "Deprecated")`<br>2. Assert HTTP 200<br>3. Parse items and assert all have `status === "Deprecated"` | Only Deprecated compliance records are returned | REGRESSION | Active |
| API-COMP-004 | listComplianceByStatus response items have required fields | 1. Send `listComplianceByStatus` with any valid status, limit=1<br>2. Assert HTTP 200<br>3. Parse first item<br>4. Assert item.id, item.firmwareId, item.status, item.certifications are all present | Each compliance item contains all required fields | SMOKE | Active |
| API-COMP-005 | getCompliance returns record by valid ID | 1. Seed a valid compliance ID from `listComplianceByStatus` in beforeAll<br>2. Send authenticated GraphQL query: `getCompliance(id: seededId)`<br>3. Assert HTTP 200<br>4. Assert returned `record.id === seededId` | Complete compliance record returned for the queried ID | SMOKE | Active |
| API-COMP-006 | getComplianceByCertification returns records for CE certification | 1. Send authenticated GraphQL query: `getComplianceByCertification(certification: "CE")`<br>2. Assert HTTP 200<br>3. Parse items and assert length > 0 | Compliance records with CE certification are returned | REGRESSION | Active |
| API-COMP-007 | getComplianceByCertification returns records for FCC certification | 1. Send authenticated GraphQL query: `getComplianceByCertification(certification: "FCC")`<br>2. Assert HTTP 200<br>3. Parse items and assert length > 0 | Compliance records with FCC certification are returned | REGRESSION | Active |
| API-COMP-008 | getComplianceByCertification returns records for UL certification | 1. Send authenticated GraphQL query: `getComplianceByCertification(certification: "UL")`<br>2. Assert HTTP 200<br>3. Parse items and assert length > 0 | Compliance records with UL certification are returned | REGRESSION | Active |
| API-COMP-009 | getComplianceByCertification returned items contain the requested cert | 1. Send authenticated GraphQL query: `getComplianceByCertification(certification: "CE")`<br>2. Assert HTTP 200<br>3. Parse items<br>4. Assert every item's `certifications` array includes `"CE"` | All returned items contain the requested certification in their array | REGRESSION | Active |
| API-COMP-010 | getCompliance with non-existent ID returns null or error | 1. Send authenticated GraphQL query: `getCompliance(id: "non-existent-comp-999")`<br>2. Assert HTTP 200<br>3. Assert `response.data.getCompliance` is null OR `response.errors` is defined | null data or descriptive error returned for unknown compliance ID | NEGATIVE | Active |
| API-COMP-011 | listComplianceByStatus with invalid status returns empty or error | 1. Send authenticated GraphQL query: `listComplianceByStatus(status: "INVALID_STATUS")`<br>2. Assert HTTP 200<br>3. Assert items is empty OR `response.errors` is defined | Invalid status returns empty items or a validation error | NEGATIVE | Active |
| API-COMP-012 | getComplianceByCertification with unknown certification returns empty | 1. Send authenticated GraphQL query: `getComplianceByCertification(certification: "UNKNOWN_CERT")`<br>2. Assert HTTP 200<br>3. Parse items and assert length === 0 | Empty items array returned for an unrecognised certification | NEGATIVE | Active |

---

## 9. API Tests — Audit Logs

**Spec File:** `specs/api/audit-logs.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-AUDIT-001 | listAuditLogs returns records for last 24 hours | 1. Compute `startDate = now - 24h`, `endDate = now` (ISO format)<br>2. Send authenticated GraphQL query: `listAuditLogs(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Assert no errors<br>5. Assert `listAuditLogs` result and items are defined | Response is HTTP 200 with items defined for the 24-hour window | SMOKE | Active |
| API-AUDIT-002 | listAuditLogs returns records for last 7 days | 1. Compute `startDate = now - 7 days`, `endDate = now`<br>2. Send authenticated GraphQL query: `listAuditLogs(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Assert no errors and items are defined | Response is HTTP 200 with items defined for the 7-day window | REGRESSION | Active |
| API-AUDIT-003 | listAuditLogs returns records for last 30 days | 1. Compute `startDate = now - 30 days`, `endDate = now`<br>2. Send authenticated GraphQL query: `listAuditLogs(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Parse items and assert length > 0 | Audit log records exist and are returned for the 30-day window | REGRESSION | Active |
| API-AUDIT-004 | listAuditLogs response items have required fields | 1. Send authenticated GraphQL query: `listAuditLogs` for last 30 days with `limit=1`<br>2. Assert HTTP 200<br>3. Parse first item<br>4. Assert `item.id` is truthy<br>5. Assert `item.action` is truthy<br>6. Assert at least one timestamp field exists (timestamp/createdAt/eventTime/date) | Each audit log item contains id, action, and a timestamp field | SMOKE | Active |
| API-AUDIT-005 | listAuditLogs with limit=5 returns at most 5 items | 1. Send authenticated GraphQL query: `listAuditLogs` for last 30 days with `limit=5`<br>2. Assert HTTP 200<br>3. Parse items array<br>4. Assert items.length ≤ 5 | Response contains at most 5 items when limit=5 is specified | REGRESSION | Active |
| API-AUDIT-006 | listAuditLogs timestamps are within requested range | 1. Compute `startDate = now - 7 days`, `endDate = now`<br>2. Send `listAuditLogs(startDate, endDate)`<br>3. Parse items<br>4. For each item assert its timestamp >= startDate and <= endDate | All returned audit log timestamps fall within the specified date range | REGRESSION | Active |
| API-AUDIT-007 | listAuditLogs totalCount is returned | 1. Send authenticated GraphQL query: `listAuditLogs` for last 30 days<br>2. Assert HTTP 200<br>3. Assert `response.data.listAuditLogs.totalCount` is a number ≥ 0 | totalCount field is present and is a non-negative integer | REGRESSION | Active |
| API-AUDIT-008 | getAuditLogsByUser returns logs for a known user | 1. Seed `existingUserId` from `listAuditLogs` in beforeAll<br>2. Send authenticated GraphQL query: `getAuditLogsByUser(userId: existingUserId)`<br>3. Assert HTTP 200<br>4. Parse items and assert at least one item is returned | Audit log entries for the specified user are returned | SMOKE | Active |
| API-AUDIT-009 | listAuditLogs returns empty for future date range | 1. Compute `startDate = now + 1 year`, `endDate = now + 2 years`<br>2. Send authenticated GraphQL query: `listAuditLogs(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Parse items and assert length === 0 | Empty items array returned for a fully future date range | NEGATIVE | Active |
| API-AUDIT-010 | listAuditLogs with reversed date range returns empty or error | 1. Set `startDate = now`, `endDate = now - 1 day`<br>2. Send authenticated GraphQL query: `listAuditLogs(startDate, endDate)`<br>3. Assert HTTP 200<br>4. Assert items is empty OR `response.errors` is defined | Reversed date range returns empty results or a validation error | NEGATIVE | Active |
| API-AUDIT-011 | getAuditLogsByUser with non-existent userId returns empty | 1. Send authenticated GraphQL query: `getAuditLogsByUser(userId: "unknown-user-999")`<br>2. Assert HTTP 200<br>3. Parse items and assert length === 0 | Empty items array returned for an unknown user ID | NEGATIVE | Active |
| API-AUDIT-012 | listAuditLogs with invalid ISO date format returns error | 1. Send authenticated GraphQL query: `listAuditLogs(startDate: "not-a-date", endDate: "also-invalid")`<br>2. Assert `response.errors` is defined (GraphQL validation or parsing error) | A GraphQL validation or parsing error is returned for malformed ISO dates | NEGATIVE | Active |

---

## 10. API Tests — Users & Customers

**Spec File:** `specs/api/users.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-USER-001 | getUserByEmail returns user profile for known email | 1. Seed a known registered email from the test environment<br>2. Send authenticated GraphQL query: `getUserByEmail(email: seededEmail)`<br>3. Assert HTTP 200<br>4. Assert no errors<br>5. Assert returned user object is defined | Complete user profile returned for the queried email address | SMOKE | Active |
| API-USER-002 | getUserByEmail response has required fields | 1. Send authenticated GraphQL query: `getUserByEmail(email: seededEmail)`<br>2. Assert HTTP 200<br>3. Assert returned `user.id`, `user.email`, `user.name`, `user.role` are all present | Returned user profile contains all required fields: id, email, name, role | SMOKE | Active |
| API-USER-003 | listUsersByRole Admin returns at least one admin user | 1. Send authenticated GraphQL query: `listUsersByRole(role: "Admin")`<br>2. Assert HTTP 200<br>3. Parse items and assert length > 0 | At least one Admin user is returned | SMOKE | Active |
| API-USER-004 | listUsersByRole returns a response for role=Admin | 1. Send authenticated GraphQL query: `listUsersByRole(role: "Admin")`<br>2. Assert HTTP 200<br>3. Assert `response.data.listUsersByRole` is defined | A valid response is returned for role=Admin | REGRESSION | Active |
| API-USER-005 | listUsersByRole returns a response for role=Technician | 1. Send authenticated GraphQL query: `listUsersByRole(role: "Technician")`<br>2. Assert HTTP 200<br>3. Assert `response.data.listUsersByRole` is defined | A valid response is returned for role=Technician | REGRESSION | Active |
| API-USER-006 | listUsersByRole returns a response for role=Manager | 1. Send authenticated GraphQL query: `listUsersByRole(role: "Manager")`<br>2. Assert HTTP 200<br>3. Assert `response.data.listUsersByRole` is defined | A valid response is returned for role=Manager | REGRESSION | Active |
| API-USER-007 | listUsersByRole items have required fields | 1. Send authenticated GraphQL query: `listUsersByRole(role: "Admin")` with `limit=1`<br>2. Assert HTTP 200<br>3. Parse first item<br>4. Assert `item.id`, `item.email`, `item.name`, `item.role` are all present | Each user item contains all required fields: id, email, name, role | REGRESSION | Active |
| API-USER-008 | getCustomerWithRelations returns data for a known customer | 1. Seed a known `customerId` from the test environment<br>2. Send authenticated GraphQL query: `getCustomerWithRelations(customerId: seededId)`<br>3. Assert HTTP 200<br>4. Assert returned customer data is non-null and includes related devices/locations | Customer record with related entities is returned for the queried ID | SMOKE | Active |
| API-USER-009 | getUserByEmail with non-existent email returns null or error | 1. Send authenticated GraphQL query: `getUserByEmail(email: "nobody@nonexistent999.com")`<br>2. Assert HTTP 200<br>3. Assert `response.data.getUserByEmail` is null OR `response.errors` is defined | null data or descriptive error returned for an unregistered email | NEGATIVE | Active |
| API-USER-010 | listUsersByRole with unknown role returns 200 | 1. Send authenticated GraphQL query: `listUsersByRole(role: "UnknownRole")`<br>2. Assert HTTP 200 (no server-side role validation enforced)<br>3. Assert response does not throw an unhandled error | HTTP 200 returned for unknown role (no server-side role enum validation) | NEGATIVE | Active |
| API-USER-011 | getCustomerWithRelations with non-existent customerId returns empty or error | 1. Send authenticated GraphQL query: `getCustomerWithRelations(customerId: "non-existent-cust-999")`<br>2. Assert HTTP 200<br>3. Assert data is null/empty OR `response.errors` is defined | null/empty data or error returned for unknown customer ID | NEGATIVE | Active |
| API-USER-012 | getUserByEmail with malformed email format returns null or error | 1. Send authenticated GraphQL query: `getUserByEmail(email: "not-an-email")`<br>2. Assert HTTP 200<br>3. Assert `response.data.getUserByEmail` is null OR `response.errors` is defined | null or validation error returned for a malformed email string | NEGATIVE | Active |

---

## 11. API Tests — Mutations

**Spec File:** `specs/api/mutations.api.spec.ts`
**Framework:** Playwright / TypeScript (GraphQL)

### createFirmware

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-MUT-001 | Creates a new firmware record with Pending status | 1. Generate unique payload: `name="Test-FW-<timestamp>"`, `version="1.0.0"`, `deviceModel="ModelX"`<br>2. Send authenticated GraphQL mutation: `createFirmware(payload)`<br>3. Assert HTTP 200<br>4. Assert returned `firmware.id` is truthy<br>5. Assert returned `firmware.status === "Pending"` | New firmware record created with Pending status and a valid ID | SMOKE | Active |
| API-MUT-002 | createFirmware with minimal required fields succeeds | 1. Build payload with only `name` and `version` fields<br>2. Send authenticated GraphQL mutation: `createFirmware(minimalPayload)`<br>3. Assert HTTP 200 or graceful skip if resolver rejects missing optional fields<br>4. Assert returned record has `id` and `status="Pending"` | Firmware creation succeeds with only the minimum required fields | REGRESSION | Active |
| API-MUT-003 | createFirmware without required name returns GraphQL error | 1. Build payload omitting the `name` field<br>2. Send authenticated GraphQL mutation: `createFirmware(payloadWithoutName)`<br>3. Assert `response.errors` is defined<br>4. Assert error message references missing `name` field | GraphQL field-required error returned when name is omitted | NEGATIVE | Active |
| API-MUT-004 | createFirmware without required version returns GraphQL error | 1. Build payload omitting the `version` field<br>2. Send authenticated GraphQL mutation: `createFirmware(payloadWithoutVersion)`<br>3. Assert `response.errors` is defined<br>4. Assert error message references missing `version` field | GraphQL field-required error returned when version is omitted | NEGATIVE | Active |

### createServiceOrder

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-MUT-005 | Creates a new service order with Pending status | 1. Generate unique payload: `title="Test-SO-<timestamp>"`, `priority="Medium"`<br>2. Send authenticated GraphQL mutation: `createServiceOrder(payload)`<br>3. Assert HTTP 200<br>4. Assert returned `order.id` is truthy<br>5. Assert returned `order.status === "Pending"` | New service order created with Pending status and a valid ID | SMOKE | Active |
| API-MUT-006 | createServiceOrder without required title returns GraphQL error | 1. Build payload omitting the `title` field<br>2. Send authenticated GraphQL mutation: `createServiceOrder(payloadWithoutTitle)`<br>3. Assert `response.errors` is defined | GraphQL field-required error returned when title is omitted | NEGATIVE | Active |

### createCompliance

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-MUT-007 | Creates a compliance submission in Pending status | 1. Seed a valid `firmwareId` from `listFirmware`<br>2. Generate unique payload: `firmwareId`, `firmwareVersion`, `deviceModel`, `certifications=["CE"]`<br>3. Send authenticated GraphQL mutation: `createCompliance(payload)`<br>4. Assert HTTP 200<br>5. Assert returned `compliance.id` is truthy<br>6. Assert returned `compliance.status === "Pending"` | New compliance record created with Pending status and a valid ID | SMOKE | Active |
| API-MUT-008 | createCompliance without required firmwareId returns error | 1. Build payload omitting the `firmwareId` field<br>2. Send authenticated GraphQL mutation: `createCompliance(payloadWithoutFirmwareId)`<br>3. Assert `response.errors` is defined | GraphQL field-required error returned when firmwareId is omitted | NEGATIVE | Active |

### updateEntityStatus

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-MUT-009 | Approves a Pending firmware record | 1. Create a new firmware record (status=Pending) via `createFirmware` mutation<br>2. Send authenticated mutation: `updateEntityStatus(entityId, entityType:"firmware", newStatus:"Approved")`<br>3. Assert HTTP 200<br>4. Assert returned entity `status === "Approved"` | Firmware record successfully transitions from Pending to Approved | SMOKE | Active |
| API-MUT-010 | Deprecates a firmware record | 1. Create a new firmware record (status=Pending) via `createFirmware` mutation<br>2. Send authenticated mutation: `updateEntityStatus(entityId, entityType:"firmware", newStatus:"Deprecated")`<br>3. Assert HTTP 200<br>4. Assert returned entity `status === "Deprecated"` | Firmware record successfully transitions to Deprecated status | REGRESSION | Active |
| API-MUT-011 | updateEntityStatus with non-existent ID returns 200 (upsert behaviour) | 1. Send authenticated mutation: `updateEntityStatus(entityId:"non-existent-id-999", entityType:"firmware", newStatus:"Approved")`<br>2. Assert HTTP 200 (backend upserts on unknown ID)<br>3. Assert no unhandled error in response | HTTP 200 returned for unknown entity ID due to upsert behaviour | REGRESSION | Active |
| API-MUT-012 | updateEntityStatus with invalid newStatus returns 200 | 1. Create a firmware record and capture its ID<br>2. Send mutation: `updateEntityStatus(entityId, entityType:"firmware", newStatus:"INVALID_STATUS")`<br>3. Assert HTTP 200 (no server-side enum validation)<br>4. Assert no unhandled error thrown | HTTP 200 returned for invalid newStatus value (no server-side enum enforcement) | NEGATIVE | Active |
| API-MUT-013 | updateEntityStatus without required entityType returns GraphQL error | 1. Build mutation payload omitting `entityType`<br>2. Send authenticated mutation: `updateEntityStatus(entityId, newStatus:"Approved")`<br>3. Assert `response.errors` is defined | GraphQL field-required error returned when entityType is omitted | NEGATIVE | Active |

### updateDeviceCoords

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| API-MUT-014 | Updates device coordinates with valid address and lat/lng | 1. Seed a valid `deviceId` from `listDevices`<br>2. Send authenticated mutation: `updateDeviceCoords(deviceId, address:"123 Test St", lat:37.7749, lng:-122.4194)`<br>3. Assert HTTP 200<br>4. Assert returned device location/coords fields are updated | Device coordinates and address are successfully updated | SMOKE | Active |
| API-MUT-015 | updateDeviceCoords without required address returns GraphQL error | 1. Seed a valid `deviceId`<br>2. Build mutation payload omitting the `address` field<br>3. Send authenticated mutation: `updateDeviceCoords(deviceId, lat:37.7749, lng:-122.4194)`<br>4. Assert `response.errors` is defined | GraphQL field-required error returned when address is omitted | NEGATIVE | Active |
| API-MUT-016 | updateDeviceCoords with out-of-range coordinates returns error | 1. Seed a valid `deviceId`<br>2. Send authenticated mutation: `updateDeviceCoords(deviceId, address:"Test", lat:999, lng:999)`<br>3. Assert `response.errors` is defined or HTTP 400 returned<br>4. Assert error references invalid coordinate range | Validation error returned when lat/lng values exceed valid geographic range | NEGATIVE | Active |

---

## Summary

| Module | SMOKE | REGRESSION | NEGATIVE | Total |
|---|---|---|---|---|
| UI — Login | 1 | 0 | 4 | 5 |
| UI — Dashboard KPI | 5 | 13 | 0 | 18 |
| UI — Dashboard API | 7 | 24 | 0 | 31 |
| UI — Geo Location | 6 | 10 | 0 | 16 |
| API — Devices | 3 | 7 | 5 | 15 |
| API — Service Orders | 3 | 5 | 5 | 13 |
| API — Firmware | 4 | 7 | 4 | 15 |
| API — Compliance | 4 | 5 | 3 | 12 |
| API — Audit Logs | 4 | 5 | 3 | 12 |
| API — Users & Customers | 4 | 4 | 4 | 12 |
| API — Mutations | 5 | 3 | 8 | 16 |
| UI — Dashboard Recent Alerts | 4 | 8 | 0 | 12 |
| UI — Dashboard System Status | 4 | 10 | 0 | 14 |
| UI — Dashboard Alerts & System Status Integration | 3 | 1 | 0 | 4 |
| **Total** | **57** | **102** | **36** | **195** |

### Status Legend

| Status | Meaning |
|---|---|
| Active | Test is implemented and included in a suite |
| Skipped | Test exists but is excluded or marked skip |
| Pending | Planned but not yet implemented |
| Deprecated | Removed / no longer relevant |

### Type Legend

| Type | Meaning |
|---|---|
| SMOKE | Core happy-path tests; must pass on every build |
| REGRESSION | Full regression coverage; run on release candidates |
| NEGATIVE | Error / boundary / auth failure scenarios |

---

## 12. UI Tests — Dashboard Recent Alerts

**Suite File:** `dashboard-alerts-suite.xml`
**Test Class:** `DashboardAlertsTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_ALERTS`
**JIRA Story:** PS-5 / QA Sub-task PS-30

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-ALERTS-01 | Recent Alerts panel is visible after login | 1. Verify div.bg-card:has(h3:text-is('Recent Alerts')) is visible<br>2. Verify panel contains text 'Recent Alerts'<br>3. Verify no KPI loading placeholder '—' remains | Alerts panel container rendered and visible; KPI cards have resolved | SMOKE | Active |
| TC-ALERTS-02 | Panel displays audit-log entries from last 24 hours | 1. Register route to inject one approved audit-log entry (listAuditLogs)<br>2. Re-navigate to Dashboard<br>3. Verify div.bg-muted alert item row is visible<br>4. Verify 'No recent activity' is NOT visible<br>5. Unroute (finally) | At least one alert item row rendered; empty-state absent | SMOKE | Active |
| TC-ALERTS-03 | Alert item displays action text and timestamp | 1. Register route to inject entry with action 'Firmware update approved'<br>2. Re-navigate to Dashboard<br>3. Verify alert item row is visible<br>4. Verify panel containsText 'Firmware update approved'<br>5. Assert JS-evaluated item text is non-empty (timestamp present)<br>6. Unroute (finally) | Action text present in panel; alert item has non-empty text including timestamp | REGRESSION | Active |
| TC-ALERTS-04 | Failed actions display orange warning icon | 1. Register route to inject failed-action entry (auditStatus='failed')<br>2. Re-navigate to Dashboard<br>3. Verify alert item row is visible<br>4. Verify svg[class*='text-orange'] is visible inside div.bg-muted<br>5. Unroute (finally) | Orange SVG icon rendered in failed-action alert row | REGRESSION | Active |
| TC-ALERTS-05 | Approved/approve actions display green check icon | 1. Register route to inject approved-action entry (auditStatus='approved')<br>2. Re-navigate to Dashboard<br>3. Verify alert item row is visible<br>4. Verify svg[class*='text-green'] is visible inside div.bg-muted<br>5. Unroute (finally) | Green SVG icon rendered in approved-action alert row | REGRESSION | Active |
| TC-ALERTS-06 | Other (info) actions display blue info icon | 1. Register route to inject info-action entry (auditStatus='info')<br>2. Re-navigate to Dashboard<br>3. Verify alert item row is visible<br>4. Verify svg[class*='text-blue'] is visible inside div.bg-muted<br>5. Unroute (finally) | Blue SVG icon rendered in info-action alert row | REGRESSION | Active |
| TC-ALERTS-07 | 'View all' link visible and navigates to /analytics | 1. Verify a[href='/analytics'] inside Alerts panel is visible<br>2. Click the 'View all' link<br>3. Assert window.location.pathname equals '/analytics' | 'View all' link present; clicking routes browser to /analytics | SMOKE | Active |
| TC-ALERTS-08 | Loading state — 'Loading alerts…' shown while API pending | 1. Register route to delay all AppSync responses by 2 s (background Thread)<br>2. Re-navigate to Dashboard<br>3. Verify 'Loading alerts' substring is visible in panel<br>4. Unroute (finally) | 'Loading alerts…' text (U+2026 ellipsis) visible while audit-log API is in-flight | REGRESSION | Active |
| TC-ALERTS-09 | Empty state — 'No recent activity' when no logs exist | 1. Register route to return empty listAuditLogs (items=[])<br>2. Re-navigate to Dashboard<br>3. Verify 'No recent activity' text is visible<br>4. Verify no div.bg-muted alert item rows are present<br>5. Verify panel container is still visible<br>6. Unroute (finally) | Empty-state message shown; no item rows; panel still rendered | REGRESSION | Active |
| TC-ALERTS-10 | Error state — div.bg-red-50 shown when audit-log API returns 500 | 1. Register route to return HTTP 500 only for listAuditLogs<br>2. Re-navigate to Dashboard<br>3. Verify div.bg-red-50 inside alerts card is visible<br>4. Verify Total Devices KPI still shows a value (KPI unaffected)<br>5. Verify global KPI error banner NOT visible<br>6. Unroute (finally) | Alerts card-level error element visible; KPI domain unaffected; no global error banner | REGRESSION | Active |
| TC-ALERTS-11 | Alerts panel isolated from KPI API failures | 1. Confirm Dashboard loaded (Total Devices visible)<br>2. Register route to return 500 for all fetch/xhr<br>3. Click Refresh Dashboard<br>4. Verify global KPI error banner appears<br>5. Verify Alerts panel container still visible<br>6. Unroute (finally) | Global error banner shown; Alerts panel still rendered — separate error domains confirmed | REGRESSION | Active |
| TC-ALERTS-12 | Alerts panel updates correctly after Refresh Dashboard | 1. Verify Alerts panel visible before refresh<br>2. Verify Refresh Dashboard button visible<br>3. Click Refresh Dashboard<br>4. Wait for KPI cards to repopulate<br>5. Verify Alerts panel still visible post-refresh<br>6. Verify no global error banner | Panel remains rendered after refresh; KPI cards repopulate; no error banner | SMOKE | Active |

---

## 13. UI Tests — Dashboard System Status

**Suite File:** `dashboard-system-status-suite.xml`
**Test Class:** `DashboardSystemStatusTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_SYSTEM_STATUS`
**JIRA Story:** PS-5 / QA Sub-task PS-30

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-STATUS-01 | System Status panel visible after login | 1. Verify div.bg-card:has(h3:text-is('System Status')) is visible<br>2. Verify panel containsText 'System Status' | Panel container rendered and heading text present | SMOKE | Active |
| TC-STATUS-02 | Panel displays exactly 4 service rows | 1. Verify SERVICE_ITEM is visible<br>2. JS evaluate count of div.space-y-4 > div — assert equals 4<br>3. Verify panel contains 'Deployment Service', 'Compliance Engine', 'Asset Database', 'Analytics Platform' | Exactly 4 rows; all 4 service names present | SMOKE | Active |
| TC-STATUS-03 | Each service row has one status label (Operational or Degraded) | 1. Wait for KPI data to load<br>2. JS evaluate count of span.text-green-600 + span.text-orange-600 — assert sum equals 4<br>3. Assert at least one label visible | Total green + orange label count equals 4; at least one visible | REGRESSION | Active |
| TC-STATUS-04 | Each service row displays a progress bar | 1. Wait for Dashboard load<br>2. JS evaluate count of progress bar elements — assert ≥ 4<br>3. Assert at least one bar has non-zero width | 4 progress bars present; at least one has non-zero computed width | REGRESSION | Active |
| TC-STATUS-05 | Analytics Platform always shows 'Operational' | 1. Wait for Dashboard load<br>2. Verify ANALYTICS_PLATFORM_OPERATIONAL (span.text-green-600) visible<br>3. Verify ANALYTICS_PLATFORM_DEGRADED (span.text-orange-600) NOT visible<br>4. Verify text 'Operational' present in row | Green label present; orange label absent; 'Operational' text confirmed | SMOKE | Active |
| TC-STATUS-06 | Deployment Service 'Operational' when in-progress orders exist | 1. Register route returning 1 in-progress service order<br>2. Re-navigate to Dashboard<br>3. Wait for Active Deployments KPI<br>4. Verify DEPLOYMENT_SERVICE_OPERATIONAL visible<br>5. Unroute (finally) | span.text-green-600 visible in Deployment Service row | REGRESSION | Active |
| TC-STATUS-07 | Deployment Service 'Degraded' when no in-progress orders | 1. Register route returning empty in-progress orders<br>2. Re-navigate to Dashboard<br>3. Wait for Active Deployments KPI<br>4. Verify DEPLOYMENT_SERVICE_DEGRADED visible<br>5. Unroute (finally) | span.text-orange-600 visible in Deployment Service row | REGRESSION | Active |
| TC-STATUS-08 | Compliance Engine 'Operational' when no pending compliance items | 1. Register route returning empty compliance items<br>2. Re-navigate to Dashboard<br>3. Wait for Pending Approvals KPI<br>4. Verify COMPLIANCE_ENGINE_OPERATIONAL visible<br>5. Unroute (finally) | span.text-green-600 visible in Compliance Engine row | REGRESSION | Active |
| TC-STATUS-09 | Compliance Engine 'Degraded' when pending compliance items exist | 1. Register route returning 1 pending compliance item<br>2. Re-navigate to Dashboard<br>3. Wait for Pending Approvals KPI<br>4. Verify COMPLIANCE_ENGINE_DEGRADED visible<br>5. Unroute (finally) | span.text-orange-600 visible in Compliance Engine row | REGRESSION | Active |
| TC-STATUS-10 | Asset Database reflects average device health score | 1. Register route with high-health devices (avg 96.5%)<br>2. Re-navigate — verify ASSET_DATABASE_OPERATIONAL visible<br>3. Switch to low-health devices (avg 50%) via Refresh Dashboard<br>4. Verify ASSET_DATABASE_DEGRADED visible<br>5. Unroute (finally) | Green label for ≥90% health; orange label for <90% health | REGRESSION | Active |
| TC-STATUS-11 | Panel visible when KPI error banner appears | 1. Confirm Dashboard loaded<br>2. Register route returning 500 for all fetch/xhr<br>3. Click Refresh Dashboard<br>4. Verify global error banner visible<br>5. Verify SystemStatus.CONTAINER still visible<br>6. Unroute (finally) | Error banner shown; System Status panel still rendered | REGRESSION | Active |
| TC-STATUS-12 | Panel updates correctly after Refresh Dashboard | 1. Verify panel visible before refresh<br>2. Click Refresh Dashboard<br>3. Wait for KPI cards to repopulate<br>4. Verify panel still visible<br>5. Assert at least one status label visible post-refresh | Panel rendered after refresh; status labels re-populated from fresh KPI data | SMOKE | Active |
| TC-STATUS-13 | Service rows appear in correct order | 1. Wait for Dashboard load<br>2. JS evaluate extract row text array — assert Row 1 contains 'Deployment Service', Row 4 contains 'Analytics Platform'<br>3. Verify nth-child(1) containsText 'Deployment Service'<br>4. Verify nth-child(4) containsText 'Analytics Platform' | Rows in order: Deployment Service → Compliance Engine → Asset Database → Analytics Platform | REGRESSION | Active |
| TC-STATUS-14 | Operational uses text-green-600, Degraded uses text-orange-600 | 1. Verify OPERATIONAL_LABEL visible; JS assert className includes 'text-green-600'<br>2. Register route with zero orders to force Deployment Service Degraded<br>3. Re-navigate — verify DEPLOYMENT_SERVICE_DEGRADED visible<br>4. Unroute (finally) | Operational span className = text-green-600; Degraded span className = text-orange-600 | REGRESSION | Active |

---

## 14. UI Tests — Dashboard Alerts & System Status Integration

**Suite File:** `dashboard-alerts-system-status-integration-suite.xml`
**Test Class:** `DashboardAlertsSystemStatusIntegrationTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_ALERTS`, `DASHBOARD_SYSTEM_STATUS`
**JIRA Story:** PS-5 / QA Sub-task PS-30

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-INTEGRATION-01 | Both panels visible simultaneously on Dashboard | 1. Verify AlertsPanel.CONTAINER is visible<br>2. Verify SystemStatus.CONTAINER is visible<br>3. Verify KpiCard.KPI_GRID_CONTAINER is visible | All three content sections coexist on Dashboard without layout breakage | SMOKE | Active |
| TC-INTEGRATION-02 | 'View all' navigates to /analytics; back-navigation works | 1. Verify VIEW_ALL_LINK visible<br>2. Click 'View all' link<br>3. Assert URL path equals '/analytics'<br>4. Launch Dashboard URL<br>5. Verify Dashboard h1 header visible | 'View all' routes to /analytics; Dashboard loads correctly on return | SMOKE | Active |
| TC-INTEGRATION-03 | Both panels render correctly after Refresh Dashboard | 1. Verify Alerts panel, System Status panel, and Total Devices KPI loaded<br>2. Click Refresh Dashboard<br>3. Wait for KPI cards to repopulate<br>4. Verify Alerts panel still visible<br>5. Verify System Status panel still visible<br>6. Verify no global error banner | Both panels rendered and functional after refresh cycle | SMOKE | Active |
| TC-INTEGRATION-04 | Zero data state — both panels handle empty APIs gracefully | 1. Register route returning empty items for all GraphQL queries<br>2. Re-navigate to Dashboard<br>3. Verify Total Devices KPI shows '0'<br>4. Verify 'No recent activity' shown in Alerts panel<br>5. Verify System Status panel visible<br>6. Verify no global error banner<br>7. Unroute (finally) | KPI shows '0'; Alerts shows empty state; System Status present; no error banner | REGRESSION | Active |

---

## 15. UI Tests — Dashboard Quick Actions with Live Counts

**Suite File:** `dashboard-quick-actions-suite.xml`
**Test Class:** `DashboardQuickActionsTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_QUICK_ACTIONS`
**JIRA Story:** PS-6 / QA Sub-task PS-31

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-PS6-QA-01 | Four Quick Action cards displayed on Dashboard | 1. Re-navigate to Dashboard for clean state<br>2. Verify 'Welcome back' h2 heading visible<br>3. Verify `main a[href='/inventory']` (View Inventory card) visible<br>4. Verify `main a[href='/account-service']` (Schedule Service card) visible<br>5. Verify `a[href='/deployment'].relative.bg-card` (Deploy Firmware card) visible<br>6. Verify `main a[href='/compliance']` (Check Compliance card) visible<br>7. JS evaluate count of all 4 card selectors — assert equals 4 | All four Quick Action cards are visible in the main content area; exactly 4 cards are present | SMOKE | Active |
| TC-PS6-QA-02 | Each Quick Action card displays a label and SVG icon | 1. Verify 'View Inventory' label text visible inside card<br>2. Verify SVG icon present inside View Inventory card<br>3. Verify 'Schedule Service' label text visible<br>4. Verify SVG icon present in Schedule Service card<br>5. Verify 'Deploy Firmware' label text visible<br>6. Verify SVG icon present in Deploy Firmware card<br>7. Verify 'Check Compliance' label text visible<br>8. Verify SVG icon present in Check Compliance card | Each card renders its descriptive label and an SVG icon element | SMOKE | Active |
| TC-PS6-NAV-01 | 'View Inventory' card navigates to /inventory | 1. Re-navigate to Dashboard<br>2. Verify View Inventory card visible<br>3. Click `main a[href='/inventory']`<br>4. Assert `browser.url()` contains '/inventory'<br>5. Verify Dashboard h1 is NOT visible (navigated away) | URL contains '/inventory'; Inventory page renders; Dashboard header absent | SMOKE | Active |
| TC-PS6-NAV-02 | 'Schedule Service' card navigates to /account-service | 1. Re-navigate to Dashboard<br>2. Verify Schedule Service card visible<br>3. Click `main a[href='/account-service']`<br>4. Assert URL contains '/account-service'<br>5. Verify Dashboard h1 NOT visible | URL contains '/account-service'; Account & Service page renders | SMOKE | Active |
| TC-PS6-NAV-03 | 'Deploy Firmware' card navigates to /deployment | 1. Re-navigate to Dashboard<br>2. Verify Deploy Firmware card visible<br>3. Click `a[href='/deployment'].relative.bg-card`<br>4. Assert URL contains '/deployment'<br>5. Verify Dashboard h1 NOT visible | URL contains '/deployment'; Deployment page renders | SMOKE | Active |
| TC-PS6-NAV-04 | 'Check Compliance' card navigates to /compliance | 1. Re-navigate to Dashboard<br>2. Verify Check Compliance card visible<br>3. Click `main a[href='/compliance']`<br>4. Assert URL contains '/compliance'<br>5. Verify Dashboard h1 NOT visible | URL contains '/compliance'; Compliance page renders | SMOKE | Active |
| TC-PS6-BADGE-01 | 'View Inventory' badge shows offline device count | 1. Re-navigate to Dashboard and wait for badges to resolve<br>2. Verify View Inventory card visible<br>3. Check if `main a[href='/inventory'] span.bg-orange-500` is visible<br>4. If visible: read text, parse as int, assert > 0<br>5. If absent: confirm offline count is 0 (badge correctly hidden) | If offline devices exist, badge is visible with a positive integer count; if none exist, badge is absent | REGRESSION | Active |
| TC-PS6-BADGE-02 | 'Schedule Service' badge shows scheduled orders count | 1. Re-navigate to Dashboard<br>2. Verify Schedule Service card visible<br>3. Check if `main a[href='/account-service'] span.bg-orange-500` is visible<br>4. If visible: parse text as int, assert > 0<br>5. If absent: confirm scheduled orders count is 0 | If scheduled orders exist, badge visible with positive integer; if none, badge absent | REGRESSION | Active |
| TC-PS6-BADGE-03 | 'Deploy Firmware' badge shows pending firmware count | 1. Re-navigate to Dashboard<br>2. Verify Deploy Firmware card visible<br>3. Check if `a[href='/deployment'].relative.bg-card span.bg-orange-500` is visible<br>4. If visible: parse text as int, assert > 0<br>5. If absent: confirm pending firmware count is 0 | If pending firmware items exist, badge visible with positive integer; if none, badge absent | REGRESSION | Active |
| TC-PS6-BADGE-04 | 'Check Compliance' badge shows pending compliance count | 1. Re-navigate to Dashboard<br>2. Verify Check Compliance card visible<br>3. Check if `main a[href='/compliance'] span.bg-orange-500` is visible<br>4. If visible: parse text as int, assert > 0<br>5. If absent: confirm pending compliance count is 0 | If pending compliance records exist, badge visible with positive integer; if none, badge absent | REGRESSION | Active |
| TC-PS6-BADGE-05 | Badges hidden when all counts are zero (API mock) | 1. Register route intercepting all 4 badge GraphQL ops to return count=0<br>2. Navigate to Dashboard with mocked responses<br>3. Verify VIEW_INVENTORY_BADGE not visible<br>4. Verify SCHEDULE_SERVICE_BADGE not visible<br>5. Verify DEPLOY_FIRMWARE_BADGE not visible<br>6. Verify CHECK_COMPLIANCE_BADGE not visible<br>7. Unroute (finally) | No orange badge spans appear on any card when all API counts are zero | REGRESSION | Disabled (requires zero-count env) |
| TC-PS6-BADGE-06 | Visible badge values are positive integers only | 1. Re-navigate to Dashboard, wait for badges to resolve<br>2. Verify welcome heading visible (page loaded gate)<br>3. JS evaluate all `main span.bg-orange-500` elements<br>4. For each: parse as int, assert not NaN, not ≤ 0, text matches integer string exactly | All visible badge elements contain parseable positive integers; no NaN, decimals, negatives, or undefined strings | REGRESSION | Active |
| TC-PS6-BADGE-07 | Sidebar badges isolated from Quick Action card badges | 1. Re-navigate to Dashboard, wait for sidebar and main content<br>2. Verify sidebar nav Inventory link visible<br>3. JS evaluate — assert no `nav span.bg-orange-500` is inside `main` and vice versa<br>4. Compare badge text for /inventory between sidebar and main card — assert match | nav-scoped and main-scoped badge selectors do not overlap; badge counts are consistent between sidebar and Quick Action card | REGRESSION | Active |
| TC-PS6-WELCOME-01 | Welcome message shows logged-in user email | 1. Re-navigate to Dashboard<br>2. Verify h2:has-text('Welcome back') visible<br>3. Verify welcome message containsText(adminUsername) — 'ajaykumar.yadav@3pillarglobal.com' | Welcome h2 contains the admin email address after login | SMOKE | Active |
| TC-PS6-WELCOME-02 | Welcome message falls back to 'Admin' when no email (session mock) | 1. Strip email attribute from Cognito session in localStorage<br>2. Reload Dashboard<br>3. Verify 'Welcome back' h2 visible<br>4. Verify h2 containsText('Admin') — fallback rendered | h2 reads 'Welcome back, Admin' when user email is absent from the session | REGRESSION | Disabled (requires session mock) |
| TC-PS6-WELCOME-03 | Welcome message format and immediate visibility | 1. Re-navigate to Dashboard<br>2. Verify h2:has-text('Welcome back') visible (no loading delay)<br>3. Verify h2 containsText('Welcome back,') — comma present<br>4. JS read h2 textContent, assert matches regex `Welcome back,\s+.+` | Welcome message is immediately visible; format is 'Welcome back, {identifier}' with correct comma and space | SMOKE | Active |
| TC-PS6-REFRESH-01 | Refresh button visible with aria-label and SVG icon | 1. Re-navigate to Dashboard<br>2. Verify `button[aria-label='Refresh dashboard']` visible<br>3. Verify `button[aria-label='Refresh dashboard'] svg` visible<br>4. JS evaluate `button.disabled` — assert false | Refresh button is visible in the welcome row, contains SVG icon, and is not disabled | SMOKE | Active |
| TC-PS6-REFRESH-02 | Refresh button re-fetches all dashboard data | 1. Re-navigate to Dashboard; verify Total Devices KPI populated before refresh<br>2. Click Refresh Dashboard<br>3. Verify Total Devices, Active Deployments, Pending Approvals, Health Score KPI values all visible after refresh<br>4. Verify no '—' loading placeholder remains<br>5. Verify no error banner | All KPI cards repopulate with valid data after refresh; no placeholders or error banners remain | REGRESSION | Active |
| TC-PS6-REFRESH-03 | Refresh button shows animate-spin class during refresh | 1. Re-navigate to Dashboard; confirm Refresh button visible<br>2. Click Refresh Dashboard<br>3. Immediately JS evaluate `button svg` class — assert contains 'animate-spin'<br>4. Wait for KPI card to repopulate (refresh complete gate)<br>5. JS evaluate `button svg` class — assert does NOT contain 'animate-spin' | SVG icon has 'animate-spin' class while refresh is active; class removed after completion | REGRESSION | Active |
| TC-PS6-REFRESH-04 | Refresh button disabled during active refresh | 1. Re-navigate to Dashboard<br>2. JS evaluate `button.disabled` before click — assert false<br>3. Click Refresh Dashboard<br>4. Immediately JS evaluate `button.disabled` — assert true<br>5. Wait for KPI repopulation (refresh complete)<br>6. JS evaluate `button.disabled` — assert false | Button has `disabled` attribute during refresh; re-enabled after completion | REGRESSION | Active |
| TC-PS6-REFRESH-05 | Badge counts valid after Refresh Dashboard | 1. Re-navigate to Dashboard; verify View Inventory card visible<br>2. Click Refresh Dashboard; wait for KPI repopulation<br>3. Verify all 4 Quick Action cards still visible<br>4. JS evaluate all `main span.bg-orange-500` — assert none contains U+2014 em-dash | All Quick Action cards present after refresh; no badge shows a loading placeholder | REGRESSION | Active |
| TC-PS6-ERR-01 | Badge values are never zero or negative | 1. Re-navigate to Dashboard; wait for badges to resolve<br>2. Verify welcome heading visible (page loaded)<br>3. JS evaluate all `main span.bg-orange-500` — for each, parseInt and assert > 0 | All visible badge values parse as integers > 0; no zero, negative, or NaN values | REGRESSION | Active |
| TC-PS6-ERR-02 | Quick Action cards accessible when GraphQL returns HTTP 500 | 1. Register route `**/graphql` → HTTP 500<br>2. Navigate to Dashboard with all APIs failing<br>3. Verify all 4 Quick Action cards still visible<br>4. JS evaluate card text — assert no 'undefined', 'NaN', 'null' strings in badge positions<br>5. Unroute (finally) | All 4 cards rendered; no raw error strings in badge positions; cards not collapsed by API failure | REGRESSION | Active |
| TC-PS6-ERR-03 | Dashboard survives hard page reload | 1. Re-navigate to Dashboard; verify welcome message and cards visible<br>2. `browser.reload()` — hard reload<br>3. Verify Dashboard h1 visible (session preserved, no login redirect)<br>4. Verify welcome message containsText(adminUsername)<br>5. Verify all 4 Quick Action cards visible<br>6. Verify Refresh button visible and not disabled | After reload: session preserved; welcome message, 4 cards, and Refresh button all re-render correctly | REGRESSION | Active |
| TC-PS6-ERR-04 | Direct URL access renders all Quick Action features | 1. `Launch.app(baseUrl)` with existing authenticated session<br>2. Verify Dashboard h1 visible (no redirect to login)<br>3. Verify welcome message visible<br>4. Verify all 4 Quick Action cards visible<br>5. Verify Refresh button visible and not disabled | Direct URL navigation renders welcome message, 4 cards, and Refresh button without re-login | REGRESSION | Active |

---

## 16. UI Tests — Dashboard E2E Integration (PS-7)

**Suite File:** `dashboard-e2e-integration-suite.xml`
**Test Class:** `DashboardE2EIntegrationTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `DASHBOARD_E2E_INTEGRATION`
**JIRA Story:** PS-7 | **QA Sub-task:** PS-32

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-PS7-KPI-01 | All four KPI cards populate from parallel API calls on page load | 1. Re-navigate to Dashboard to trigger fresh page load<br>2. Verify 'Welcome back' h2 heading visible<br>3. Verify Total Devices, Active Deployments, Pending Approvals, Health Score KPI card values all visible<br>4. Verify no '—' (U+2014) loading placeholder remains in any KPI card<br>5. Verify no red error banner (div.bg-red-50) is present | All 4 KPI cards populated from 6 parallel API calls; no placeholder; no error banner | SMOKE | Active |
| TC-PS7-KPI-02 | KPI values display valid non-negative data — no JS artifacts | 1. Verify Total Devices, Active Deployments, Pending Approvals values visible<br>2. Assert Health Score value containsText('%') — percentage format confirmed<br>3. JS evaluate all div[class*='text-3xl'] textContent joined<br>4. Assert result is non-empty and contains no 'NaN', 'undefined', or 'null' | All KPI values are valid; Health Score has '%' suffix; no JavaScript error artifacts in any card | REGRESSION | Active |
| TC-PS7-KPI-03 | KPI cards show em-dash loading placeholder during API fetch delay | 1. Register route to delay all AppSync GraphQL responses by 2 s via Thread.sleep<br>2. Re-navigate to Dashboard to trigger fresh page load<br>3. Assert KpiCard.LOADING_PLACEHOLDER ('—' U+2014) is visible immediately after navigation<br>4. Unregister all route handlers (finally block) | At least one KPI card displays '—' while six API calls are artificially delayed | REGRESSION | Active |
| TC-PS7-ALERTS-01 | Recent Alerts panel displays exactly 3 entries from 24-hour window | 1. Register route to inject 3-entry mock (approved/failed/info) for listAuditLogs<br>2. Re-navigate to Dashboard with mock active<br>3. Verify AlertsPanel.CONTAINER visible<br>4. JS evaluate count of div.bg-muted rows — assert equals 3<br>5. Verify AlertsPanel.EMPTY_STATE is NOT visible<br>6. Unregister routes (finally) | Exactly 3 div.bg-muted alert item rows rendered; 'No recent activity' absent | REGRESSION | Active |
| TC-PS7-ALERTS-02 | Recent Alerts panel shows empty state when no audit logs exist | 1. Register route to inject empty items[] for listAuditLogs<br>2. Re-navigate to Dashboard<br>3. Verify AlertsPanel.CONTAINER still visible<br>4. Verify AlertsPanel.EMPTY_STATE ('No recent activity') is visible<br>5. JS evaluate count of div.bg-muted rows — assert equals 0<br>6. Unregister routes (finally) | 'No recent activity' text visible; zero div.bg-muted rows; panel container still present | REGRESSION | Active |
| TC-PS7-ALERTS-03 | 'View all' link in Recent Alerts panel navigates to /analytics | 1. Verify AlertsPanel.VIEW_ALL_LINK (a[href='/analytics']) is visible<br>2. Click 'View all' link via DashboardPageImpl.clickViewAllAlerts()<br>3. JS evaluate window.location.pathname<br>4. Assert pathname equals '/analytics' | Clicking 'View all' navigates to the /analytics route | SMOKE | Active |
| TC-PS7-ICON-01 | Failed audit entry renders exclusive orange warning SVG icon | 1. Inject single auditStatus='failed' entry via listAuditLogs mock<br>2. Re-navigate to Dashboard<br>3. Verify AlertsPanel.FAILED_ACTION_ICON (svg[class*='text-orange']) visible<br>4. Verify AlertsPanel.APPROVED_ACTION_ICON NOT visible<br>5. Verify AlertsPanel.INFO_ACTION_ICON NOT visible<br>6. Unregister routes (finally) | Orange SVG icon exclusive to failed entry; no green or blue icon in same row | REGRESSION | Active |
| TC-PS7-ICON-02 | Approved audit entry renders exclusive green check SVG icon | 1. Inject single auditStatus='approved' entry via listAuditLogs mock<br>2. Re-navigate to Dashboard<br>3. Verify AlertsPanel.APPROVED_ACTION_ICON (svg[class*='text-green']) visible<br>4. Verify AlertsPanel.FAILED_ACTION_ICON NOT visible<br>5. Unregister routes (finally) | Green SVG icon exclusive to approved entry; no orange icon in same row | REGRESSION | Active |
| TC-PS7-ICON-03 | Info audit entry renders exclusive blue info SVG icon | 1. Inject single auditStatus='info' entry via listAuditLogs mock<br>2. Re-navigate to Dashboard<br>3. Verify AlertsPanel.INFO_ACTION_ICON (svg[class*='text-blue']) visible<br>4. Verify no orange or green icon in same row<br>5. Unregister routes (finally) | Blue SVG icon exclusive to info entry; no orange or green icon in same row | REGRESSION | Active |
| TC-PS7-ICON-04 | Each alert item row displays action text and a timestamp | 1. Inject one approved entry with action 'Firmware update approved'<br>2. Re-navigate to Dashboard<br>3. Verify AlertsPanel.ALERT_ITEM (div.bg-muted) visible<br>4. Verify AlertsPanel.CONTAINER containsText('Firmware update approved')<br>5. JS evaluate first div.bg-muted textContent — assert non-empty (includes time ref)<br>6. Unregister routes (finally) | Action text and non-empty timestamp both present in alert item row | REGRESSION | Active |
| TC-PS7-BADGE-01 | All four Quick Action cards rendered with labels, icons, and badges | 1. Re-navigate to Dashboard<br>2. Verify VIEW_INVENTORY_CARD_MAIN, SCHEDULE_SERVICE_CARD_MAIN, DEPLOY_FIRMWARE_CARD, CHECK_COMPLIANCE_CARD_MAIN all visible<br>3. JS evaluate count of `main div[class*='grid'] > a` — assert 4<br>4. Verify SVG icon visible in each card | Exactly 4 Quick Action cards; each has label, icon, and badge span | SMOKE | Active |
| TC-PS7-BADGE-02 | View Inventory card shows correct offline device badge count (3) | 1. Inject 3 offline devices for listDevices(Offline) mock<br>2. Re-navigate to Dashboard<br>3. Verify QuickActions.VIEW_INVENTORY_BADGE (span.bg-orange-500) visible<br>4. JS evaluate badge textContent — assert '3'<br>5. Unregister routes (finally) | View Inventory badge displays '3' matching the 3 injected offline devices | REGRESSION | Active |
| TC-PS7-BADGE-03 | Schedule Service card shows correct scheduled orders badge count (5) | 1. Inject 5 scheduled orders for listServiceOrdersByStatus(Scheduled) mock<br>2. Re-navigate to Dashboard<br>3. Verify QuickActions.SCHEDULE_SERVICE_BADGE visible<br>4. JS evaluate badge textContent — assert '5'<br>5. Unregister routes (finally) | Schedule Service badge displays '5' matching the 5 injected scheduled orders | REGRESSION | Active |
| TC-PS7-BADGE-04 | Deploy Firmware card shows correct pending firmware badge count (2) | 1. Inject 2 pending firmware records for listFirmware(Pending) mock<br>2. Re-navigate to Dashboard<br>3. Verify QuickActions.DEPLOY_FIRMWARE_BADGE visible<br>4. JS evaluate badge textContent — assert '2'<br>5. Unregister routes (finally) | Deploy Firmware badge displays '2' matching the 2 injected pending firmware records | REGRESSION | Active |
| TC-PS7-BADGE-05 | Check Compliance card shows correct pending compliance badge count (4) | 1. Inject 4 pending compliance records for listFirmwareCompliance(Pending) mock<br>2. Re-navigate to Dashboard<br>3. Verify QuickActions.CHECK_COMPLIANCE_BADGE visible<br>4. JS evaluate badge textContent — assert '4'<br>5. Unregister routes (finally) | Check Compliance badge displays '4' matching the 4 injected pending compliance records | REGRESSION | Active |
| TC-PS7-BADGE-06 | Quick Action cards navigate to correct module routes | 1. Re-navigate to Dashboard<br>2. Click View Inventory → JS evaluate pathname, assert '/inventory'<br>3. Return to Dashboard; click Schedule Service → assert '/account-service'<br>4. Return; click Deploy Firmware → assert '/deployment'<br>5. Return; click Check Compliance → assert '/compliance' | All 4 Quick Action cards navigate to their correct routes | SMOKE | Active |
| TC-PS7-STATUS-01 | Deployment Service shows Operational (green) when active orders exist | 1. Inject 1 In-Progress order for listServiceOrdersByStatus(In Progress)<br>2. Re-navigate to Dashboard<br>3. Verify SystemStatus.CONTAINER visible<br>4. Verify SystemStatus.DEPLOYMENT_SERVICE_OPERATIONAL (span.text-green-600) visible<br>5. Verify SystemStatus.DEPLOYMENT_SERVICE_DEGRADED NOT visible<br>6. Unregister routes (finally) | Deployment Service row shows green 'Operational' with active orders | REGRESSION | Active |
| TC-PS7-STATUS-02 | Deployment Service shows Degraded (orange) when no active orders exist | 1. Inject empty orders for listServiceOrdersByStatus(In Progress)<br>2. Re-navigate to Dashboard<br>3. Verify SystemStatus.DEPLOYMENT_SERVICE_DEGRADED (span.text-orange-600) visible<br>4. Verify SystemStatus.DEPLOYMENT_SERVICE_OPERATIONAL NOT visible<br>5. Unregister routes (finally) | Deployment Service row shows orange 'Degraded' with zero active orders | REGRESSION | Active |
| TC-PS7-STATUS-03 | System Status panel renders exactly 4 service rows in fixed order | 1. JS evaluate count of `div.space-y-4 > div` in System Status panel — assert 4<br>2. Verify panel containsText 'Deployment Service', 'Compliance Engine', 'Asset Database', 'Analytics Platform'<br>3. Verify SystemStatus.ANALYTICS_PLATFORM_OPERATIONAL visible<br>4. Verify SystemStatus.ANALYTICS_PLATFORM_DEGRADED NOT visible | Exactly 4 service rows; all 4 names present; Analytics Platform always Operational | REGRESSION | Active |
| TC-PS7-STATUS-04 | Compliance Engine shows Degraded when pending compliance items exist | 1. Inject 1 Pending compliance item for listFirmwareCompliance mock<br>2. Re-navigate to Dashboard<br>3. Verify SystemStatus.COMPLIANCE_ENGINE_DEGRADED (span.text-orange-600 in Compliance Engine row) visible<br>4. Unregister routes (finally) | Compliance Engine row shows orange 'Degraded' when pending compliance items are present | REGRESSION | Active |
| TC-PS7-REFRESH-01 | Refresh Dashboard button is visible and accessible | 1. Verify DashboardPage.REFRESH_DASHBOARD_BUTTON (button[aria-label='Refresh dashboard']) visible<br>2. JS evaluate button.disabled — assert false (button is enabled) | Refresh button present in welcome header row and is enabled | SMOKE | Active |
| TC-PS7-REFRESH-02 | Clicking Refresh re-triggers all API calls; KPI cards repopulate | 1. Verify no loading placeholder before click (stable state)<br>2. Click Refresh Dashboard button<br>3. Wait for KpiCard.LOADING_PLACEHOLDER to appear (refresh in progress)<br>4. Wait for placeholder to disappear (refresh complete)<br>5. Verify all 4 KPI card values visible<br>6. Verify no error banner | All KPI cards repopulated after manual refresh; no error banner | SMOKE | Active |
| TC-PS7-REFRESH-03 | Refresh SVG icon applies animate-spin class during active refresh cycle | 1. Register route to delay all AppSync responses by 2 s<br>2. Click Refresh Dashboard<br>3. Immediately JS evaluate `button[aria-label='Refresh dashboard'] svg` class — assert contains 'animate-spin'<br>4. Wait for placeholder to disappear (refresh complete)<br>5. JS evaluate SVG class again — assert does NOT contain 'animate-spin'<br>6. Unregister routes (finally) | SVG has 'animate-spin' during refresh; class removed on completion | REGRESSION | Active |
| TC-PS7-REFRESH-04 | Dashboard data updates after refresh when backend data changed | 1. Register route returning 1 device for listDevices (non-offline) — simulates changed data<br>2. Click Refresh Dashboard<br>3. Wait for placeholder to appear then disappear (refresh complete)<br>4. JS evaluate Total Devices div[class*='text-3xl'] textContent — assert '1'<br>5. Unregister routes (finally) | Total Devices KPI displays '1' after refresh reflecting the updated mock backend data | REGRESSION | Active |
| TC-PS7-ERR-01 | Red error banner displayed when all KPI API calls fail with HTTP 500 | 1. Register route returning HTTP 500 for all AppSync requests<br>2. Re-navigate to Dashboard<br>3. Verify ErrorBanner.CONTAINER (div.p-6.space-y-6 > div.bg-red-50) visible<br>4. Verify ErrorBanner.MESSAGE containsText('network error')<br>5. Unregister routes (finally) | Red error banner with network error message visible when all KPI APIs return 500 | REGRESSION | Active |
| TC-PS7-ERR-02 | KPI cards fall back to '0' on full API failure — no crash or blank screen | 1. Register route returning HTTP 500 for all AppSync requests<br>2. Re-navigate to Dashboard<br>3. Wait for KpiCard.ZERO_FALLBACK ('0') to become visible<br>4. Verify no '—' loading placeholder remains<br>5. Verify Total Devices and Health Score card containers present<br>6. JS evaluate !!document.querySelector('h1') — assert true (no blank screen)<br>7. Unregister routes (finally) | KPI cards show '0' gracefully; no crash overlay; Dashboard layout intact | REGRESSION | Active |
| TC-PS7-ERR-03 | Audit-log API failure does not trigger global KPI error banner | 1. Register route returning HTTP 500 ONLY for listAuditLogs; all other requests pass through<br>2. Re-navigate to Dashboard<br>3. Verify ErrorBanner.CONTAINER is NOT visible (global banner absent)<br>4. Verify KpiCard.TOTAL_DEVICES_VALUE visible (KPI domain unaffected)<br>5. Assert alerts panel shows in-panel error (AlertsPanel.ERROR_MESSAGE) OR empty state (AlertsPanel.EMPTY_STATE)<br>6. Unregister routes (finally) | No global error banner; KPI cards populated; alerts panel handles its own failure in isolation | REGRESSION | Active |
| TC-PS7-ERR-04 | Error banner disappears after successful manual refresh following failure | 1. Register route returning HTTP 500 for all AppSync requests<br>2. Re-navigate to Dashboard — confirm ErrorBanner.CONTAINER visible<br>3. Remove all error routes via browser.unrouteAll()<br>4. Click Refresh Dashboard<br>5. Wait for KPI placeholder to disappear (refresh complete)<br>6. Verify ErrorBanner.CONTAINER is NOT visible | Error banner dismissed after a successful manual refresh; error state is recoverable | REGRESSION | Active |
| TC-PS7-ERR-05 | Unauthenticated access to Dashboard redirects to login page | 1. Clear all cookies via browser.context().clearCookies()<br>2. Clear localStorage and sessionStorage via JS evaluate<br>3. Navigate directly to base URL<br>4. Verify LoginPage.EMAIL_FIELD visible (redirected to login)<br>5. Verify DashboardPage.DASHBOARD_HEADER is NOT visible | Unauthenticated navigation redirects to login; Dashboard is not exposed | SMOKE | Active |

---

## 17. UI Tests — Deployment Page: Firmware Status Badges (PS-34 / AC-4)

**Suite File:** `firmware-status-suite.xml`
**Test Class:** `FirmwareStatusTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `FIRMWARE_STATUS`
**JIRA Story:** PS-34 / QA Sub-task PS-41

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-34.4.05-001 | Deployment page loads with Firmware Versions tab active | 1. Navigate to /deployment via sidebar nav link<br>2. Verify button#tab-firmware is visible<br>3. Verify button#tab-audit is visible<br>4. Verify [aria-labelledby='tab-firmware'] panel is visible | Firmware Versions tab is rendered and active; Audit Log tab button is also present | SMOKE | Active |
| TC-34.4.05-002 | Firmware cards and status badges are rendered in the tab panel | 1. Navigate to Deployment page<br>2. Verify at least one .bg-card exists inside the firmware tabpanel<br>3. Verify at least one span.inline-flex.rounded-full badge is visible<br>4. Verify at least one h3.text-lg card heading is visible | At least one firmware card with a status badge is rendered | SMOKE | Active |
| TC-34.4.05-003 | Approved firmware cards render a green status badge (bg-green-100) | 1. Navigate to Deployment page<br>2. Verify span.bg-green-100 is visible inside [aria-labelledby='tab-firmware'] .bg-card | At least one Approved (green) badge is present | REGRESSION | Active |
| TC-34.4.05-004 | Pending firmware cards render an orange status badge (bg-orange-100) | 1. Navigate to Deployment page<br>2. Verify span.bg-orange-100 is visible inside [aria-labelledby='tab-firmware'] .bg-card | At least one Pending (orange) badge is present | REGRESSION | Active |
| TC-34.4.05-005 | Deprecated firmware cards render a red status badge (bg-red-100) | 1. Navigate to Deployment page<br>2. Verify span.bg-red-100 is visible inside [aria-labelledby='tab-firmware'] .bg-card | At least one Deprecated (red) badge is present | REGRESSION | Active |
| TC-34.4.05-006 | Deprecated firmware cards show only the Details toggle (no Download or Deprecate) | 1. Navigate to Deployment page<br>2. Verify span.bg-red-100 (Deprecated card) is visible<br>3. Verify button.btn-primary (Download) is NOT visible<br>4. Verify button.border-red-300 (Deprecate) is NOT visible<br>5. Verify button[aria-expanded] (Details toggle) is visible | Deprecated cards do not expose Download or Deprecate buttons; only Details is available | REGRESSION | Active |
| TC-34.4.05-007 | Approved firmware cards expose Download and Deprecate buttons | 1. Navigate to Deployment page<br>2. Verify span.bg-green-100 (Approved card) is visible<br>3. Verify button.btn-primary (Download) is visible<br>4. Verify button.border-red-300 (Deprecate) is visible | Approved cards show both Download and Deprecate action buttons | REGRESSION | Active |
| TC-34.4.05-008 | Clicking Audit Log tab switches content and shows Export button | 1. Navigate to Deployment page<br>2. Click button#tab-audit<br>3. Wait for Export Full Log button to appear<br>4. Verify button:has-text('Export Full Log') is visible<br>5. Verify [aria-labelledby='tab-firmware'] panel is NOT visible | Audit Log panel loads; firmware cards no longer visible | REGRESSION | Active |
| TC-34.4.05-009 | [POST-PS-34] Only new 5-value enum badges visible after PS-34 ships | 1. Navigate to Deployment page<br>2. Verify span:has-text('Screening') is visible<br>3. Verify span:has-text('Active') is visible<br>4. Verify span:has-text('Pending') is NOT visible<br>5. Verify span:has-text('Approved') is NOT visible<br>6. Verify span:has-text('Rejected') is NOT visible | New enum values (Screening, Staged, Active, Deprecated, Recalled) only; legacy values absent | REGRESSION | Pending-PS-34 |
| TC-34.4.05-010 | [POST-PS-34] Recalled terminal-state cards show only Details button | 1. Navigate to Deployment page<br>2. Verify span:has-text('Recalled') is visible<br>3. Verify button.btn-primary NOT visible<br>4. Verify button.border-red-300 NOT visible | Recalled cards show only Details toggle; no Download or Deprecate | REGRESSION | Pending-PS-34 |
| TC-34.4.05-011 | [POST-PS-34] Active firmware cards expose Download and Deprecate buttons | 1. Navigate to Deployment page<br>2. Verify span:has-text('Active') is visible<br>3. Verify button.btn-primary visible<br>4. Verify button.border-red-300 visible | Active cards (post-PS-34 equivalent of Approved) show correct action buttons | REGRESSION | Pending-PS-34 |

---

## 18. UI Tests — Deployment Page: Upload Firmware Form Validation (PS-34 / AC-7)

**Suite File:** `upload-firmware-suite.xml`
**Test Class:** `UploadFirmwareTests`
**Groups:** `SMOKE_TESTS`, `REGRESSION`, `UPLOAD_FIRMWARE`
**JIRA Story:** PS-34 / QA Sub-task PS-41

| TC ID | Scenario | Test Steps | Expected Result | Type | Status |
|---|---|---|---|---|---|
| TC-34.7.07-001 | Upload Firmware button is visible on Deployment page | 1. Navigate to /deployment<br>2. Verify button:has-text('Upload Firmware') is visible | Upload Firmware btn-primary button is present | SMOKE | Active |
| TC-34.7.07-002 | Clicking Upload Firmware opens modal with all form fields | 1. Navigate to Deployment page<br>2. Click Upload Firmware button<br>3. Wait for [role='dialog'] to appear<br>4. Verify modal title 'Upload Firmware' visible<br>5. Verify input#fw-name, #fw-version, #fw-device-model, #fw-manufacturer, input#fw-file, textarea#fw-release-notes all visible<br>6. Click Cancel to close | Modal opens with correct title and all 6 form fields visible | SMOKE | Active |
| TC-34.7.07-003 | Submit button is disabled when all fields are empty | 1. Open Upload Firmware modal<br>2. JS evaluate `document.querySelector('[role="dialog"] button[type="submit"]').disabled`<br>3. Assert disabled === true<br>4. Cancel modal | Submit button has HTML disabled attribute with all fields empty | SMOKE | Active |
| TC-34.7.07-004 | Submit button remains disabled with only Name filled | 1. Open modal<br>2. Type name into input#fw-name<br>3. JS evaluate Submit button disabled — assert true<br>4. Cancel | Submit button still disabled; Version, Device Model, File still empty | REGRESSION | Active |
| TC-34.7.07-005 | Submit button remains disabled without firmware file | 1. Open modal<br>2. Fill Name, Version, Device Model<br>3. JS evaluate Submit button disabled — assert true (file not attached)<br>4. Cancel | Submit button disabled until firmware file is selected | REGRESSION | Active |
| TC-34.7.07-006 | Submit button becomes enabled after all required fields are filled | 1. Open modal<br>2. Fill Name, Version, Device Model<br>3. Create temp binary file and set via browser.setInputFiles('input#fw-file', path)<br>4. JS evaluate Submit button disabled — assert false (enabled)<br>5. Cancel (do not submit to backend) | Submit button transitions to enabled after Name + Version + Device Model + File provided | REGRESSION | Active |
| TC-34.7.07-007 | Cancel button closes modal without submitting | 1. Open modal<br>2. Verify [role='dialog'] visible<br>3. Click Cancel button<br>4. Verify [role='dialog'] NOT visible<br>5. Verify Upload Firmware button still visible on page | Modal dismissed cleanly; user remains on Deployment page | SMOKE | Active |
| TC-34.7.08-001 | [POST-PS-34 UI] Firmware Family dropdown is present in Upload form | 1. Open Upload Firmware modal<br>2. Verify label:has-text('Firmware Family') visible<br>3. Verify select#fw-family-id (or equivalent combobox) visible<br>4. Cancel | Firmware Family field present — AC-3 UI exposure confirmed | REGRESSION | Pending-PS-34 |
| TC-34.7.08-002 | Baseline gap check — Firmware Family field is currently absent | 1. Open Upload Firmware modal<br>2. Verify label:has-text('Firmware Family') is NOT visible<br>3. Cancel | Field absent in pre-PS-34 UI — AC-3 gap documented; test passes to confirm baseline | REGRESSION | Active |
